# 历史兼容对象本月清理待办

记录时间：2026-07-28 17:01:00  
目标时间：2026-07 月底前完成一轮集中清理  
当前状态：2026-07-29 已完成本月历史兼容尾巴集中清理。已按“先备份、先迁移关系和证据、再物理删除、最后服务器复核”的口径处理，不改 Oracle。

## 0. 2026-07-29 服务器只读核对结论

本文件记录的是“本月清理范围”，不等于服务器当前仍然存在全部问题。2026-07-29 已按服务器 Neo4j 只读核对，实际剩余问题已明显收敛。

本次收口的直接背景不是“做 CDSS 导入包”，而是上周已经完成 CDSS 基础字典对照映射后，图谱主数据需要同步进入稳定状态：疾病要对齐标准诊断，检查/检验/药品/手术/治疗项目要尽量对齐 CDSS 基础字典，无法对齐的进入待审队列；历史旧实体、旧标签、重复主数据和废弃对象不能继续干扰后续专病扩展。

| 核对项 | 当前结果 | 结论 |
|---|---:|---|
| `DiseaseClassification` 历史实体 | 0 | 已清零 |
| `LabTest` 历史实体 | 0 | 已清零 |
| 旧大写关系 `USES_MEDICATION`、`HAS_PROCEDURE`、`HAS_CLINICAL_MANIFESTATION` | 0 | 已清零 |
| AMI 标准诊断关系 | AMI、STEMI、NSTEMI 均已绑定 `StandardDiagnosis` | 已完成 |
| 心肌病标准诊断关系 | 12/12 已绑定 `StandardDiagnosis` | 已完成 |
| 旧 `Exam` 标签节点 | 0 | 已清零 |
| 旧 `ExamIndicator` 标签节点 | 0 | 已清零 |
| 废弃节点 | 0 | 已清零 |
| 同类型同名核心重复 | 活跃 `超声心动图` 节点 1 个 | 已迁移并删除重复节点 |
| 旧批次 Braunwald 风险分层节点 | 0 | 已迁移到 `RISK-CARD-CAD-UA-BRAUNWALD` 并删除旧节点 |
| 推荐来源裁决过程节点 `SourceAdjudication` | 0 | 548 个已迁移到推荐陈述字段和直接证据关系后物理删除 |
| 旧来源裁决过程关系 `has_source_adjudication`、`decides_recommendation` | 0 | 已清零，后续禁止再生成 |
| 孤立来源章节 `SourceSection` 空壳 | 0 | 7 个已物理删除；仍有关联的来源章节保留为证据定位锚点 |
| 审核、迁移、批次、导入类过程实体关键词复核 | 0 | 全库只剩有关联的 `SourceSection` 来源章节锚点，不属于过程垃圾 |

本轮已处理上述剩余项，不再按本文件第 2 章全量范围重复返工；后续只把这些闸门纳入新批次入库后复核。

## 1. 清理目标

把 V1.x 解析和早期迁移遗留的旧实体、旧关系、重复节点、已废弃对象集中清理一轮，避免 CDSS 导入和图谱维护长期依赖过滤条件隐藏脏数据。

核心原则：

```text
先确认已有新结构替代
再迁移关系和证据
再生成备份和回滚清单
最后物理删除
删除后必须做服务器复核
```

## 2. 本月清理对象

| 类别 | 待清理对象 | 为什么要清理 | 删除前要求 |
|---|---|---|---|
| 旧实体类型 | `DiseaseClassification` | V2.0 后不再使用，容易和疾病分型混淆。 | 确认疾病目录和诊断分型已迁移到 `DiseaseCategory`、`DiseaseSubcategory`、`Disease`。 |
| 旧检查实体 | `Exam` | 已被 `ExamItem` 和 `ExamObservation` 替代。 | 确认检查项目和检查发现关系已迁移。 |
| 旧检验实体 | `LabTest` | 已被 `LabItem` 和 `LabSubitem` 替代。 | 确认检验项目和检验细项关系已迁移。 |
| 旧指标实体 | `ExamIndicator` | V2.x 已拆分为检查发现和检验细项。 | 确认每条指标已归入 `ExamObservation` 或 `LabSubitem`。 |
| 旧大写关系 | `USES_MEDICATION`、`HAS_PROCEDURE`、`HAS_CLINICAL_MANIFESTATION` | 早期关系命名不符合小写下划线标准，容易被旧导入脚本误读。 | 确认已有新关系替代。 |
| 旧直连关系 | `requires_exam_item`、`requires_lab_item` | V2.x 标准路径改为“疾病 → 辅助检查方案 → 检查/检验项目 → 发现/细项”。 | 确认标准路径不缺失。 |
| 废弃节点 | `status=deprecated` 且无正式链路依赖的节点 | 长期留存会污染统计和导入。 | 确认无临床推荐、证据、标准主数据依赖。 |
| 重复节点 | 同类型同名、同编码、同标准 UUID 重复节点 | 会导致前端展示重复、CDSS 回填不确定。 | 确认保留主节点、迁移全部关系和证据。 |

## 3. 不允许直接删除的对象

以下对象必须先迁移或人工确认，不能一刀删：

1. 仍被疾病、方案、推荐陈述、临床规则引用的节点。
2. 仍挂有唯一证据链且没有新节点承接的节点。
3. 名称重复但编码、标准字典 UUID 或临床含义未确认的节点。
4. 与正式 CDSS 推荐链路有关的节点或关系。
5. 删除后会导致 AMI、心肌病、冠心病、心衰、心律失常、高血压、瓣膜病、肺动脉高压、起搏治疗相关疾病覆盖率下降的对象。

## 4. 执行步骤

1. 只读盘点：统计旧实体、旧关系、废弃节点、重复节点数量和涉及疾病。
2. 分类判断：分为可直接删除、需迁移后删除、需人工确认、暂不处理四类。
3. 生成清单：输出 `历史兼容对象清理清单.csv`。
4. 生成回滚包：输出 `历史兼容对象删除回滚清单.jsonl`。
5. 迁移关系和证据：先把业务关系、证据链、标准主数据引用迁移到新结构。
6. 物理删除：只删除已通过硬闸门的对象。
7. 服务器复核：检查疾病数量、关系数量、正式推荐链路、标准诊断、检查检验路径、证据链是否异常。
8. CDSS导入复核：确认导入侧不再出现历史兼容对象和解析治理属性模型节点。

## 5. 必须通过的硬闸门

| 闸门 | 验收要求 |
|---|---|
| 疾病目录闸门 | 专科、疾病大类、疾病亚类、疾病三层不断链。 |
| 标准诊断闸门 | 可诊断疾病仍能连接有效 `StandardDiagnosis`。 |
| 检查检验路径闸门 | 检查项目、检查发现、检验项目、检验细项仍能按标准路径下钻。 |
| 治疗动作闸门 | 治疗方案仍能下钻到药品、手术或治疗项目。 |
| 证据链闸门 | 删除旧节点后，证据不丢失、不孤立。 |
| 正式推荐闸门 | `RecommendationStatement` 到推荐动作和证据链不断裂。 |
| CDSS导入闸门 | 普通 CDSS 模型不再导入历史兼容对象和 AI解析治理字段。 |
| 回滚闸门 | 每条物理删除都有可执行回滚依据。 |

## 6. 预期输出物

```text
历史兼容对象清理清单.csv
历史兼容对象删除回滚清单.jsonl
历史兼容对象物理清理报告_202607xx.md
历史兼容对象服务器复核结果_202607xx.json
CDSS导入过滤复核报告_202607xx.md
```

## 7. 当前待办状态

| 待办 | 状态 |
|---|---|
| 建立本月清理任务 | 已记录 |
| 只读盘点历史兼容对象 | 已完成一次服务器只读核对 |
| 生成清理清单和回滚清单 | 已完成，见 `项目管理中心_project_management/149_历史兼容尾巴一次性收口_20260729` |
| 迁移关系和证据 | 已完成 |
| 物理删除 | 已完成 |
| 服务器复核 | 已完成 |
| CDSS导入复核 | 不在本轮执行；本轮输出图谱设计与过滤清单，由 CDSS 导入同事执行 |
## 2026-07-30 00:12:00 收口状态更新

已完成并写入服务器 Neo4j：

1. `SourceAdjudication` 历史来源裁决过程节点已迁移到正式推荐陈述字段、证据和指南关系，过程节点已物理删除。
2. 孤立 `SourceSection` 已清理；仍保留的 `SourceSection` 仅作为有证据引用的来源锚点，不属于待删垃圾节点。
3. 鉴别诊断旧规则关系已补齐当前硬闸门识别关系，本轮修复 5 条，服务器复核“鉴别诊断无规则”为 0。
4. 正式推荐动作误连检验细项已修复，本轮迁移 45 条，服务器复核“正式推荐动作错误终点”为 0。

后续清理原则：

- 只要是纯解析过程、纯审核过程、旧临时桥接、无临床实体意义且无正式证据引用的对象，本月清理时优先进入物理删除候选。
- 有证据引用、能解释临床知识来源的锚点可以保留，但不得进入普通 CDSS 元模型配置展示。
- 不再把历史遗留问题作为新疾病扩展的前置阻断；新疾病扩展只受当前硬闸门和正式 CDSS 链路质量控制。
