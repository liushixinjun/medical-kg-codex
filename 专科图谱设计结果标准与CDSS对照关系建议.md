# 专科图谱设计结果标准与CDSS对照关系建议

版本：V1.0
日期：2026-07-29
定位：给 CDSS 开发和维护同事使用的设计交付说明。

## 1. 责任边界

本项目负责提供专科知识图谱的设计结果标准、实体类型标准、属性标准、关系标准、CDSS 元模型对照建议和过滤建议。CDSS 实际导入、页面配置、后台表写入、缓存刷新、生产发布由 CDSS 开发同事完成。

我们不直接负责：

```text
CDSS 导入程序开发
CDSS Oracle 元模型表写入
CDSS 页面改造
CDSS 缓存刷新
CDSS 生产发布
```

我们负责交付：

```text
图谱 Schema 标准
实体类型对照建议
业务属性对照建议
实体关系对照建议
不应进入普通元模型的过滤清单
AMI 和心肌病样板验证口径
```

## 2. 同事优先读取文件

| 顺序 | 文件 | 用途 |
|---:|---|---|
| 1 | `专科知识图谱Schema标准.md` | 总标准，说明图谱有哪些实体、属性、关系和硬规则。 |
| 2 | `CDSS图谱元模型配置过滤清单.md` | CDSS 元模型配置对照主文件，说明哪些映射、哪些新增、哪些过滤。 |
| 3 | `专科CDSS图谱查询接口与Cypher示例.md` | 后续接口查询和专病路径调用参考。 |

## 3. 总体设计原则

专科图谱不是把旧 CDSS 的“疾病、症状、检查、检验、药品、手术”简单扩几列，而是形成完整专科诊疗知识结构。CDSS 已有实体类型、属性和关系优先复用；CDSS 没有的专科、疾病大类、疾病亚类、标准诊断、诊断标准明细、检查发现、检验细项、路径阶段、临床规则、推荐陈述、证据链、指南来源、适用人群、禁忌条件、随访计划、预后信息，按 Schema 标准新增。

导入和维护时必须按三个依据判断：

```text
entityType 判断实体类型
code 判断业务唯一对象
Schema 关系编码判断实体之间的业务关系
```

禁止直接扫描 Neo4j 的所有属性并生成 CDSS 元模型节点。

## 4. 三类对照关系

### 4.1 实体类型对照

CDSS 元模型中的“节点分类”对应 Schema 的实体类型，不对应具体业务实例。

例子：

```text
Disease 是节点分类，可以进入 CDSS 元模型。
急性心肌梗死 是 Disease 类型下的业务数据，不是节点分类。
```

### 4.2 业务属性对照

CDSS 元模型中的“属性配置”只维护业务属性，例如名称、编码、别名、定义、适用人群、禁忌说明、推荐等级、证据等级。

不能维护 AI 解析过程字段，例如批次编号、来源哈希、解析版本、迁移状态、修复状态。

### 4.3 实体关系对照

CDSS 元模型中的“关系配置”只维护实体到实体的业务关系。

例子：

```text
DiseaseCategory -> has_disease -> Disease
Disease -> has_clinical_subtype -> Disease
Disease -> has_exam_plan -> ExamPlan
ExamPlan -> includes_exam_item -> ExamItem
ExamItem -> exam_item_has_observation -> ExamObservation
TreatmentPlan -> includes_procedure -> Procedure
Procedure -> has_standard_procedure -> StandardProcedure
RecommendationStatement -> recommends_action -> Procedure
```

## 5. 不进入普通元模型配置的内容

以下内容不进入 CDSS 普通图谱元模型配置页：

```text
AI 解析字段
批次字段
哈希字段
迁移字段
修复字段
回滚字段
历史兼容实体
旧大写关系
证据全文和来源细节
```

这些内容可以保留在 Neo4j、治理日志、审计表、依据详情页或回滚文件中，但不能作为 CDSS 普通业务模型节点和业务属性。

## 6. 对 CDSS 同事的最终建议

执行时按这个顺序：

```text
先看 Schema 是否定义。
再看 CDSS 是否已有对应元模型配置。
已有则映射复用。
没有则按 Schema 新增。
最后按过滤清单排除 AI 解析治理字段和历史兼容对象。
```

如果不确定某个对象能不能进 CDSS 元模型配置页，按三句话判断：

```text
它是实体类型、业务属性、业务关系：可以进入。
它是具体疾病、症状、体征、检查项目、检查发现、检验项目、检验细项、药品、手术、治疗项目、治疗方案、随访、预后、预防、并发症、危险因素：不进元模型分类，但进入业务数据。
它是批次、哈希、解析、迁移、修复、回滚字段：不进入普通元模型配置。
```
