# 治疗方案动作链全库治理报告 20260716

## 处理结论

- 全库治疗方案节点：259 个。
- 疾病直接关联治疗方案关系：344 条。
- 推荐陈述直接推荐治疗方案：208 条。
- 本轮自动正式化“已批量签收且证据链完整”的治疗推荐：19 条。
- 正式治疗推荐从 7 条提升到 26 条；按推荐动作行计算，正式治疗推荐链为 28 条。
- 344 条 `疾病 → 治疗方案` 直连关系全部已标记为“仅知识浏览、非正式推荐入口”。
- 服务器入库后复测通过，阻断项 0。
- 专项复核通过：正式治疗推荐链缺规则、阶段、疾病或证据 = 0；直连关系未标记非正式入口 = 0。

## 本轮没有做的事

- 没有删除治疗方案节点。
- 没有批量把 510 条待使用效果审核/AI预审推荐改成正式推荐。
- 没有为了清零硬造“疾病 → 治疗方案 → 药物/手术/检查”关系。

## 核心规则

正式 CDSS 推荐区只能使用：

```text
推荐陈述 → 推荐动作 → 证据
```

完整业务链路应为：

```text
疾病 → 专病路径 → 诊疗阶段 → 临床规则 → 推荐陈述 → 推荐动作 → 证据
```

`疾病 → 治疗方案` 直连关系只允许用于图谱探索和知识浏览，不得作为正式推荐入口。

## 本轮正式化范围

只处理满足以下条件的推荐：

1. 已有推荐动作；
2. 已有证据；
3. 已连接临床规则；
4. 已连接诊疗阶段；
5. 已能回到具体疾病；
6. 原状态为临床批量签收。

本轮满足条件的 19 条主要来自高血压批次，详见：

- `03_可安全正式化_已批量签收推荐链.csv`

## 前端使用要求

正式推荐区不得再通过 `Disease -[:has_treatment_plan]-> TreatmentPlan` 查询治疗推荐。

应使用如下逻辑：

```cypher
MATCH (d:Disease {code:$disease_code})
  -[:has_clinical_pathway]->(:ClinicalPathway)
  -[:has_pathway_stage]->(:PathwayStage)
  -[:has_recommendation_statement]->(rec:RecommendationStatement)
  -[:recommends_action]->(action)
WHERE coalesce(rec.formal_cdss_ready,false)=true
   OR coalesce(rec.cdss_release_level,'')='formal'
   OR coalesce(rec.clinical_review_status,'')='clinical_ready'
OPTIONAL MATCH (rec)-[:derived_from|supported_by_evidence]->(e:Evidence)
RETURN rec, action, collect(DISTINCT e) AS evidence
```

图谱探索页如展示 `Disease -[:has_treatment_plan]-> TreatmentPlan`，必须明确标识为“知识浏览”，不能放到正式推荐区。

## 关键产物

- `01_全库治疗方案动作链治理扫描汇总.json`
- `02_疾病直连治疗方案全量扫描.csv`
- `03_可安全正式化_已批量签收推荐链.csv`
- `04_治疗方案动作链全库治理写库结果.json`
- `05_写库前_疾病直连治疗方案关系备份.json`

## 后续处理

剩余待使用效果审核或 AI 预审推荐，不直接改正式。后续应按病种批次继续补齐：

- 触发条件；
- 适用人群；
- 禁忌/排除；
- 推荐等级；
- 证据等级；
- 原文摘要；
- 对应患者字段。

