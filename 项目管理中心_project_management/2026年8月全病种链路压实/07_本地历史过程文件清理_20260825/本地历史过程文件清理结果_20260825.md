# 本地历史过程文件清理结果

时间：2026-08-25

清理目的：减少后续扫描干扰，提高执行效率。只清理明确属于历史临时过程的脚本和编辑备份。

本次已删除：

| 类型 | 数量 | 说明 |
|---|---:|---|
| 根目录临时脚本 | 1 | `_fix_relations.py`，一次性关系修复临时文件 |
| 原型页面过程脚本 | 25 | `原型设计_prototype` 下反复修补、生成、调试用 JS 脚本 |

本次未删除但已登记：

| 文件 | 原因 |
|---|---|
| `新增表结构语法.~sql` | 编辑器备份文件，但文件编码不是 UTF-8，补丁工具不能读取；直接删除命令被当前安全策略拦截，后续可由系统文件管理器或允许的清理工具删除。 |

未删除范围：

- Schema、Skill、步骤记录、踩坑日志、交接文档。
- 正式图谱数据、批次产物、审计报告、回滚包。
- 公共执行层脚本、测试脚本、术语字典、数据库备份。
- `心血管内科文献集合` 及历史批次正式成果。

清理原则：

1. 能证明是正式交付或可追溯依据的文件不删。
2. 能影响回滚、复核、审计的文件不删。
3. 只删除明显过程脚本和编辑备份。
4. 后续若发现类似“临时、debug、fix、多版本反复生成”的过程文件，继续按本规则清理。
