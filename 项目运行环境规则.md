# 项目运行环境规则

更新时间：2026-07-09

## 一、Python 标准环境

项目标准 Python 环境统一使用：

```text
D:\Program Files Ai\python-venvs\medical-kg\Scripts\python.exe
```

后续所有正式项目脚本、批次解析脚本、质量审计脚本、Neo4j 读写脚本，均优先使用该环境。

## 二、Python 依赖安装规则

如果执行项目脚本时发现缺少 Python 包：

1. 优先安装到项目专用环境：

```text
D:\Program Files Ai\python-venvs\medical-kg
```

2. 不再把长期项目依赖安装到 Codex 缓存 Python。
3. Codex 缓存 Python 只允许作为临时运行环境，不作为项目长期依赖环境。
4. 之前已误装到 Codex 缓存 Python 的依赖不迁移，不作为正式项目环境依据。

## 三、外部工具安装规则

后续如需安装 Poppler、Tesseract、Java、Node、Graphviz 等外部工具，统一安装到：

```text
D:\Program Files Ai
```

禁止默认安装到 C 盘用户目录或系统目录，除非工具强制要求且已明确记录原因。

## 四、本轮核验结果

2026-07-09 已核验：

```text
python = D:\Program Files Ai\python-venvs\medical-kg\Scripts\python.exe
python-docx = 已安装
pdfplumber = 已安装
pypdf = 已安装
neo4j = 已安装
```

## 五、执行要求

后续脚本运行记录、步骤记录、交接文件中，必须明确写出使用的 Python 路径。  
如果发现使用了 Codex 缓存 Python，需要在执行结果中说明原因，并在下一轮改回项目标准 Python。
