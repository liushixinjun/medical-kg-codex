# Schema V1.12 Governance Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `专科知识图谱Schema标准.md` 从 V1.11 升级为 V1.12 草案口径，解决实际图谱与标准不一致、9.2–9.5 不可读、教材骨架实体未收编和历史大写关系兼容问题。

**Architecture:** 只改标准文档、审计白名单和迁移说明文件，不写 Neo4j，不迁移服务器数据。Schema 负责定义标准口径；审计脚本负责允许新标准实体；迁移表负责后续 dry-run。

**Tech Stack:** Markdown、CSV、Python unittest、Neo4j 只读统计 JSON。

## Global Constraints

- 不写 Neo4j。
- 不迁移服务器数据。
- 不删除历史数据。
- 新关系标准统一小写 snake_case。
- 本次重点解决 Schema V1.11 与服务器实际数据不一致。

---

### Task 1: Schema V1.12 文档治理

**Files:**
- Modify: `E:/BigMouse/0.CDSS文献诊疗指南材料PDF/AI专科知识图谱生成/专科知识图谱Schema标准.md`

**Interfaces:**
- Consumes: `Schema冗余与优化评估报告_20260710.md`
- Produces: V1.12 标准说明、9.2–9.5 中文可读版本、实体成熟度说明

- [ ] Step 1: 将版本从 V1.11 改为 V1.12。
- [ ] Step 2: 新增 V1.12 变更记录。
- [ ] Step 3: 收编 `Definition`、`DefinitionComponent`、`DiagnosisCriteriaComponent`、`Prevention`、`SourceSection`。
- [ ] Step 4: 将 9.2–9.5 改为“白话解释 + 字段表 + 示例 + 硬规则”。

### Task 2: 审计白名单同步

**Files:**
- Modify: `E:/BigMouse/0.CDSS文献诊疗指南材料PDF/AI专科知识图谱生成/scripts/audit_graph_instance.py`
- Test: `E:/BigMouse/0.CDSS文献诊疗指南材料PDF/AI专科知识图谱生成/tests/test_audit_graph_instance.py`

**Interfaces:**
- Consumes: V1.12 新实体
- Produces: 本地批次审计允许教材骨架实体，不再把实际骨架实体误判为 unknown

- [ ] Step 1: 在 `ALLOWED_ENTITY_TYPES` 加入 V1.12 收编实体。
- [ ] Step 2: 在 `ALLOWED_DIRECTIONS` 加入小写标准关系。
- [ ] Step 3: 加测试覆盖 `DefinitionComponent` 和 `DiagnosisCriteriaComponent`。
- [ ] Step 4: 运行单元测试。

### Task 3: 迁移说明文件

**Files:**
- Create: `E:/BigMouse/0.CDSS文献诊疗指南材料PDF/AI专科知识图谱生成/Schema实体关系兼容迁移表_20260711.csv`
- Create: `E:/BigMouse/0.CDSS文献诊疗指南材料PDF/AI专科知识图谱生成/Schema字段统一规则_20260711.md`

**Interfaces:**
- Consumes: `Schema实际使用统计_20260710.json`、`Schema实际使用样例_20260710.json`
- Produces: 后续 dry-run 数据迁移依据

- [ ] Step 1: 生成实体兼容迁移表。
- [ ] Step 2: 生成字段统一规则。
- [ ] Step 3: 明确本次不执行服务器迁移。

