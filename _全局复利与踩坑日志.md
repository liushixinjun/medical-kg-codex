## 2026-07-18 06:35:00——不能用关键词粗暴判断 blocks_action，必须看推荐语义和关系类型

问题：

- 在正式推荐接口样例抽查时发现，部分推荐文本包含“禁忌证”字样，但语义是“根据适应证、禁忌证选择治疗”，不是“禁止执行该动作”。
- 如果仅凭关键词把这类推荐改成 `blocks_action`，前端会把正常推荐错误展示为禁忌/阻断。

固化规则：

1. `blocks_action` 只用于明确“不得、不推荐、禁忌阻断、未排除风险前不可执行”的推荐。
2. “根据禁忌证选择治疗”“结合适应证和禁忌证评估”仍属于正式推荐，不得自动判为阻断。
3. 前端不得自行用关键词判断动作性质，必须读取图谱关系类型：`recommends_action` 或 `blocks_action`。
4. 后续入库后复测必须抽查 `blocks_action` 明细，而不是只统计数量。

本次处理：

- 修正 6 条误判阻断关系，改回 `recommends_action`。
- 修正后服务器正式来源裁决仍为 548 条，其中建议执行 546 条，禁忌/阻断 2 条。

## 2026-07-17 23:50:00——推荐动作编码必须回到主数据实体，不能只看 RecommendationStatement 字段

问题：

- 推荐陈述里记录的 `action_code` 可能来自历史批次临时编码。
- 主数据里同一药物、手术、治疗方案已有标准实体和标准编码。
- 如果只看 `RecommendationStatement.action_code`，会出现“推荐有动作名称，但找不到动作节点”的断链。

固化规则：

1. 正式 CDSS 推荐入库前，必须校验 `action_code` 能匹配到真实 `Medication / Procedure / TreatmentPlan / ClinicalRule` 等动作实体。
2. 如果动作名称能匹配到主数据实体，应回写推荐陈述的标准动作编码，并把旧编码保留到 `legacy_action_codes`。
3. 不允许为了通过闸门新建同名假动作节点。
4. 前端展示动作时，以主数据实体的中文标准名为准，缩写放入别名。

本次处理：

- 批量修复 15 条动作编码映射。
- 补入库 14 条推荐来源裁决。
- 最终本批推荐来源裁决达到 543 条，缺动作节点为 0。

## 2026-07-17 23:50:00——禁忌/阻断类推荐必须使用 blocks_action，不得混入正向推荐

问题：

- 禁忌证、排除条件、阻断规则如果用正向 `recommends_action` 表示，医生端会误以为系统主动推荐该动作。
- 这会直接影响正式 CDSS 推荐区的安全性。

固化规则：

1. 推荐文本、规则名称或动作名称包含“禁忌、阻断、不推荐”等含义时，动作关系必须使用 `blocks_action`。
2. 正式推荐区展示时，`recommends_action` 进入“推荐做什么”，`blocks_action` 进入“不能做什么/需排除什么”。
3. 入库后必须分别统计 `recommends_action` 与 `blocks_action`，不能只看总动作关系数。

本次处理：

- 本批来源裁决最终生成 14 条 `blocks_action` 有向关系，其中来源裁决到阻断动作 7 条、推荐陈述到阻断动作 7 条。

## 2026-07-17 17:36:00——正式推荐入库复核必须用“有方向关系”统计，避免关系数翻倍

问题：

- AMI 推荐来源裁决正式入库后，第一次服务器复核使用 `MATCH ()-[r]-()` 统计关系。
- 该写法会把同一条关系按两个方向各统计一次，导致关系数翻倍，容易误判为重复写入。

固化规则：

1. 入库后关系复核必须使用有方向关系统计：`MATCH ()-[r]->()`。
2. 正式报告中只记录有方向关系数量。
3. 如果关系数异常，先区分“统计口径翻倍”和“真实重复关系”，不得直接下结论。

本次处理：

- 已修正 AMI 来源裁决正式入库脚本。
- 复核结果：5 条 SourceAdjudication、5 条 RecommendationStatement、40 条有向关系，缺主证据/推荐等级/证据等级/动作编码均为 0。

## 2026-07-17 17:36:00——历史节点 code 字段存在字符串和数组混用，正式入库脚本必须兼容

问题：

- AMI 的 PCI/经皮冠状动脉介入治疗相关历史节点中，部分 `code` 字段不是单一字符串，而是历史合并后的数组。
- 如果只按 `n.code = $code` 查询，会漏匹配；如果前端只展示残留疾病专属缩写节点，会误以为数据没有修好。

固化规则：

1. 新增节点必须使用单一标准 `code`。
2. 历史节点查询和入库脚本必须兼容字符串/数组 code，并优先按标准名称和实体类型兜底解析。
3. 前端正式展示应优先使用主数据节点，不展示已废弃或疾病专属残留节点。
4. 历史残留节点进入物理删除候选前，必须先确认没有有效关系和证据链引用。

本次处理：

- AMI 来源裁决正式入库脚本已通过名称 + 实体类型兜底解析动作节点，避免 PCI 类动作漏连。

## 2026-07-17 17:10:00——多指南推荐不能靠大模型黑箱裁决，必须落到来源裁决表和权重规则

问题：

- 同一疾病或同一临床问题可能同时解析到多份指南、共识、教材和外部权威资料。
- 如果直接把多份来源混合成一个推荐，医生不知道到底按哪份指南执行。
- 如果让大模型自行判断主依据，结果不可解释、不可复核、不可维护。

固化规则：

1. 正式 CDSS 推荐必须进入“推荐来源裁决”流程。
2. 裁决必须基于指南台账、推荐候选表、裁决规则权重表和推荐来源裁决表。
3. 大模型只负责抽取候选、场景、证据和来源，不得黑箱决定主依据。
4. 裁决结果必须保留主依据、支持依据、冲突状态、分数明细和裁决理由。
5. 新指南发布时，不重建疾病骨架；只重抽受影响证据、重算或复核来源裁决，并将旧推荐标记为“已过时保留”或“仅知识展示”。

本次处理：

- Schema 升级到 V1.17。
- SKILL 升级到 V2.5。
- 已新增 4 个模板表和 AMI 推荐来源裁决样板包。

## 2026-07-17 15:10:00——证据可用不等于正式CDSS可推荐，必须区分知识浏览链和推荐动作链

问题：

- 入库后如果只看 Evidence 数量，会误以为新增证据全部可用于正式 CDSS 推荐。
- 实际上 Evidence 可能已经能追溯指南、能支持临床实体，但还没有完整落到“规则 -> 推荐动作”。
- 全库扫描“疾病 -> 任意实体 -> Evidence”宽路径会拖慢 Neo4j，不适合作为常规审计口径。

固化规则：

1. 证据使用效果必须分三层判断：
   - 证据追溯层：`Guideline -> Evidence`。
   - 疾病知识层：`Disease -> ClinicalRule -> Evidence` 或疾病下实体卡片按需查证据。
   - 正式推荐层：`Disease -> ClinicalRule -> has_recommended_action -> Action` 且 `ClinicalRule -> Evidence`。
2. 正式 CDSS 推荐区不得直接展示疾病下全部 Evidence，也不得把疾病直连治疗方案当正式推荐。
3. 全局审计不做“疾病 -> 任意实体 -> Evidence”的宽路径全图扫描；普通实体证据应在单病种、单卡片点击时按需查询。
4. 正式推荐动作链覆盖率低于 75% 的批次，不应整体标记为正式 CDSS 推荐完成，只能分规则/分动作进入。

本次处理：

- 7 个补充批次 Evidence 指南追溯、临床实体使用、疾病主链可达均为 100%。
- 正式推荐动作链总体覆盖率 51.4%。
- 心衰 LVAD/右心衰覆盖率仅 6.9%，只能作为知识浏览和证据补强，不能整体进入正式推荐区。

## 2026-07-17 14:48:00——补充证据入库必须关系轻量化，并禁止错误全库笛卡尔统计

问题：

- 补充证据合并包关系里原本可能携带 `evidence_text` 或大段证据数组，如果原样写入，会把 Evidence 原文重复复制到大量关系上，造成数据库膨胀和入库变慢。
- 初版入库脚本的全库计数写成 `MATCH (n) OPTIONAL MATCH ()-[r]->()`，会形成节点×关系的笛卡尔统计，在大图谱上直接卡住。
- 仅靠 Python 批量写入不够，Neo4j 必须有 `KGNode.code` 索引，否则按 code 合并节点和匹配关系端点会越来越慢。

固化规则：

1. Evidence 节点保存证据原文；关系层只保存 `evidence_ids`、`document_ids`、`source_names`、页码、推荐等级等轻量索引。
2. 入库脚本不得在关系属性中写入 `evidence_text`、`provenance_records_json` 这类大字段。
3. 全库统计必须分段聚合，禁止写成节点与关系的笛卡尔组合。
4. 所有 Neo4j 入库脚本必须先确认 `KGNode.code` 索引存在且 ONLINE。
5. 入库后服务器复核必须检查：本轮关系层 `evidence_text` 残留数为 0、重复 code 为 0、Evidence 缺原文为 0。

本次处理：

- 7 个补充证据合并包已入库。
- 本轮关系层 `evidence_text` 残留：0。
- `KGNode.code` 索引：ONLINE。
- 服务器复核阻断项：0。

## 2026-07-17 13:30:00——补充解析阶段不得把教材和外部权威放入 source_roots

问题：

- G0 通用准备脚本会完整扫描 `source_roots` 下所有支持格式文件。
- 未解析 PDF 启动池第一次修正后虽然只把指南目录改成了精确 PDF 源文件夹，但仍保留了教材目录和外部权威目录。
- 结果 G0 来源清单把《内科学》、外部权威 PDF/HTML/CSV/JSON 等历史文件也纳入，造成批次污染风险。

固化规则：

1. “补充解析启动池”阶段只允许扫描本批次精确 PDF 源文件夹。
2. 不得把真实教材目录、外部权威目录、术语字典目录放入本阶段 `source_roots`。
3. 如果 G0 通用闸门要求教材路径非空，只能使用空目录满足路径校验，不能扫描真实教材。
4. 教材和外部权威只能在后续“证据补强/冲突裁决/缺口补齐”阶段按需引用。
5. 每次 G0 后必须核对：`included_file_count` 是否等于精确源 PDF 数；不相等必须停止。

本次处理：

- 已修正 `执行_未解析PDF病种启动池_G0与PDF解析_20260717.py`。
- 7 个补充批次最终 G0 全部通过，纳入文件数与精确源 PDF 数一致。
- 本次不写 Neo4j。

## 2026-07-17 06:55:00——补充解析不能直接扫总指南目录，必须使用精确 PDF 源文件夹

问题：

- 未解析 PDF 启动池第一次执行时，G0 准备逻辑会把 `scope_target` 自动加入别名。
- 如果 source_roots 指向“心血管内科/诊疗指南”总目录，就会因为疾病名、简称、别名命中而误纳入更多 PDF。
- 典型风险：原本只想补 1 份 ACS 2025 指南，却可能把冠心病相关多份 PDF 一起纳入。

固化规则：

1. 补充解析、单份指南补充、未解析 PDF 启动池，不得直接扫总指南目录。
2. 必须先建立“精确 PDF 源文件夹”，只复制本批次确认要解析的 PDF。
3. G0 阶段必须输出“本批次实际纳入 PDF 清单”，并与人工清单逐项核对。
4. 若实际纳入 PDF 数量大于预期，必须停止，不得继续 PDF 解析、证据抽取或入库。
5. 该问题属于流程闸门问题，不是单个病种问题，后续所有补充批次都适用。

本次处理：

- 已将未解析 PDF 启动池改为“精确 PDF 源文件夹”模式。
- 用户准备关机，已停止半执行进程。
- 回公司后必须先复核两个半执行目录，再重新运行启动池。

## 2026-07-16 23:55:00——废弃旧缩写节点不能继续占用知识浏览层统计口径

问题：
- V1.2 证据基线扫描发现 `CTA` 节点缺证据。
- 复核后确认该节点已经是废弃旧缩写节点，且没有疾病关系；标准表达已由“CT血管造影/冠状动脉CT血管成像”承接。
- 如果继续让废弃节点保留 `cdss_usage_scope='knowledge_browse_only'`，统计会把它当成有效知识浏览节点，造成假缺口。

固化规则：
1. 已废弃、已替换、旧缩写、旧编码节点不得继续保留在知识浏览层统计口径中。
2. 缩写节点原则上不物理删除；先设置为隐藏废弃口径，保留别名和替代节点指向，避免历史关系追溯断裂。
3. 若节点被误标为废弃，但仍有疾病关系和证据链，应先改回有效状态；不能因为状态字段错误而隐藏有效知识。
4. 知识浏览层复测必须保证 `status='deprecated'` 的节点数量为 0；旧节点统一进入 `deprecated_hidden`。
5. 若知识浏览节点本身有明确相关正式推荐陈述，可以补挂该推荐陈述已验证的证据链，但不得因此自动转入正式 CDSS 推荐区。
6. 正式 CDSS 推荐区仍只读取“推荐陈述 → 推荐动作 → 证据”，不能读取知识浏览层标题节点。

本次处理：
- `CTA` 旧缩写节点退出知识浏览层，保留别名和替代节点指向。
- `房性心律失常与卒中风险管理` 补挂 3 条指南证据。
- `胸部X线` 从误标废弃改回有效状态。
- V1.2 入库后复测知识浏览层缺证据节点为 0，知识浏览层内废弃节点为 0。

## 2026-07-16 23:35:00——不能把知识浏览节点硬造为正式推理节点

问题：
- V1.1 基线扫描显示检查、治疗方案、风险分层等仍有大量“无下游结构”的节点。
- 其中一部分是真缺口，可以补为检查指标、风险组件、禁忌阻断关系。
- 另一部分只是教材中的宽泛检查名称、宽泛治疗方案标题或历史抽取句子，不能硬连假动作链，否则正式 CDSS 会误推荐。

固化规则：
1. 正式 CDSS 推理节点必须有可计算结构：检查指标、诊断组件、阈值规则、风险组件、推荐动作、禁忌阻断或证据链。
2. 教材未明确细化的检查、风险分层、治疗方案，保留为知识浏览节点，设置 `cdss_usage_scope='knowledge_browse_only'`。
3. 前端正式推荐区不得读取知识浏览节点。
4. 规则引擎不得把“疾病直连治疗方案”当正式推荐入口。
5. 英文简称不得作为主名称；主名称用中文全称，英文简称进入 aliases。

本次处理：
- 195 条检查指标关系补齐。
- 4 个检验误分修正。
- 6 个操作/手术简称改中文全称并保留别名。
- 41 条风险分层组件补齐。
- 33 条禁忌阻断动作关系补齐。
- 45 个检查、78 个风险分层、12 个治疗方案明确挡在知识浏览层。
- V1.1 正式推理层服务器复测阻断项 0。

## 2026-07-16 22:55:00——教材骨架冻结必须按“教材范围”和“指南扩展范围”分层验收

问题：
- 周末做过骨架清洗和 SKILL 升级，但这次冻结仍暴露出高血压、瓣膜病、心包病、感染性心内膜炎、心血管神经症等相关缺口。
- 根因不是 SKILL 没升级，而是旧数据不会因为流程升级自动变干净；必须跑迁移、归并、补证据和来源重分类。
- 旧审计口径把教材来源、指南来源、专家共识、外部权威网页混在一起，导致“教材冻结缺口”和“指南精修缺口”混淆。
- 部分历史 Definition 只有文本片段，没有 source_name、source_section_path、Evidence 证据链，甚至出现从鉴别段落截出的错误定义。

固化规则：
1. 教材骨架冻结只验收《内科学》第10版心血管内科章节范围。
2. 指南、共识、外部权威内容必须保留，但不得标成教材来源。
3. 任何 Definition 进入教材骨架层，必须同时具备：定义文本、教材名称、章节路径、段落锚点或页码、Evidence 关系。
4. 新流程升级后，历史图谱必须执行迁移脚本；不能假设旧数据自动符合新标准。
5. 冻结报告必须明确“不纳入本次冻结范围”的疾病或扩展主题，避免误判为缺口。
6. 检查项目节点可以作为 V1.0 骨架存在；但正式 CDSS 诊断推理必须下钻到检查指标、阈值规则或诊断组件。

本次处理：
- 5 个冠心病旧定义按 DOCX 原文重写。
- 29 条教材定义补齐证据链。
- 14 条指南/共识/外部权威定义纠正来源类型。
- 同类型同名实体重复清零。
- 《内科学》第10版心血管内科骨架 V1.0 服务器复测通过。

## 2026-07-16 21:30:00——PowerShell 编码显示问题不得当作文件问题反复汇报

问题：
- PowerShell 默认读取中文文件时可能显示乱码，但文件本身使用 UTF-8 读取正常。
- 这类内部工具显示问题反复汇报，会耽误进度、消耗上下文和 token。

固化规则：
1. 读取中文 Markdown、JSON、CSV 时默认指定 UTF-8。
2. 只有确认文件内容本身损坏、无法被 UTF-8 正常读取，才向用户汇报。
3. 单纯 PowerShell 显示乱码不再作为阶段进度、风险或问题输出。
4. 以后排查编码问题必须在后台快速处理，不占用主线汇报。

## 2026-07-16 21:25:00——待处理状态不能长期阻断正式 CDSS，必须用机器化准入规则闭环

问题：
- 全库存在 502 条“结构完整但仍待使用效果审核”的推荐。
- 如果继续等待专家逐条审核，项目会停在人工确认环节，无法形成可持续入库机制。
- 专家适合审核简明规则、临床路径和使用效果，不适合逐条查看图谱网络边。

固化规则：
1. “待使用效果审核”不能作为长期阻断状态。
2. 正式 CDSS 推荐准入必须机器化：疾病、阶段、临床规则、触发条件、适用人群、排除/禁忌、患者字段、推荐等级、证据等级、推荐原文/摘要、证据链全部齐全。
3. 满足准入条件的推荐自动转正式；不满足的必须输出具体缺口，不能只写 pending。
4. 临床使用效果反馈作为后续迭代，不阻断结构和证据已经完整的推荐进入正式区。
5. 前端正式推荐区只能读取正式准入通过的推荐陈述，并展示当前推荐自己的证据链。

本次处理：
- 57 条推荐和 57 条规则补齐字段。
- 31 条批量签收推荐转正式。
- 502 条结构完整推荐按机器化准入转正式。
- 8 条结构缺口补链。
- 最终全库 567 条推荐全部通过正式 CDSS 准入，入库后复测阻断项 0。

## 2026-07-16 20:05:00——治疗方案直连只能用于知识浏览，不能进入正式推荐区

问题：
- 全库仍有 344 条 `疾病 → 治疗方案` 直连关系。
- 这些关系对图谱探索有用，但如果前端拿它们作为正式 CDSS 推荐入口，会绕过触发条件、禁忌、推荐陈述和证据链。

固化规则：
1. `Disease -[:has_treatment_plan]-> TreatmentPlan` 只允许作为知识浏览关系。
2. 正式 CDSS 推荐必须走 `RecommendationStatement -[:recommends_action]-> action`。
3. 医生查看推荐依据时，只展示当前推荐陈述自己的证据，不展示疾病证据池。
4. 已有疾病直连治疗方案不得物理删除，除非明确是错误关系；默认标记为非正式推荐入口。
5. 待使用效果审核或 AI 预审推荐不能一键转正式；只能把“已批量签收、动作/规则/阶段/疾病/证据齐全”的推荐转正式。

本次处理：
- 19 条已批量签收治疗推荐转为正式。
- 344 条直连关系全部标记为“仅知识浏览、非正式推荐入口”。
- 入库后复测和专项复核均通过。

## 2026-07-16 19:10:00——治疗方案已有下游药物不等于CDSS可执行推荐链

问题：
- 18 条专项宽口径问题显示“疾病关联了治疗方案”，但并不能直接用于 CDSS 推荐。
- 典型问题是“控制心室率”这类通用治疗方案虽然已有药物类别下游，但如果没有适用场景、触发条件、排除/禁忌、推荐陈述和证据，就不能直接推荐给医生。
- 早期图谱为了补知识覆盖，保留了一些“疾病 → 治疗方案”的直连关系；这会让前端误以为该疾病天然适用该治疗方案。

固化规则：
1. 正式 CDSS 推荐必须从“命中的临床规则/推荐陈述”进入，不得直接从“疾病 → 治疗方案”进入。
2. 治疗方案要进入正式推荐，必须形成完整链路：疾病 → 专病路径 → 诊疗阶段 → 临床规则 → 推荐陈述 → 推荐动作 → 证据。
3. 没有证据链的疾病到治疗方案直连，必须清理或阻断展示；不能为了清零硬造药物、检查、手术关系。
4. 通用方案如“控制心室率”必须放在具体场景下使用，例如房颤、房扑、二尖瓣狭窄合并房颤、心衰合并快速心室率等。
5. 前端正式推荐区应查询 RecommendationStatement，再展示其 recommends_action 和 evidence；不要把 Disease 直连 TreatmentPlan 当正式推荐。

本次处理：
- 新增正式动作链 5 条，正式化旧动作链 1 条。
- 删除误导性直连关系 12 条，治疗方案节点本身未删除。
- 服务器入库后复测通过；18 条专项复核剩余阻断 0。

## 2026-07-16 18:50:00——治疗方案标题不能等同于可执行推荐，必须有下游动作链

问题：
- 专项宽口径检查发现 18 条 `TreatmentPlan` 虽然被疾病关联，但没有药物、手术、检查、禁忌、规则或证据下游动作。
- 这类节点常是“控制心室率”等知识标题，不能直接展示给医生当作可执行推荐。

固化规则：
1. 正式 CDSS 推荐必须来自命中的 `RecommendationStatement` 或 `ClinicalRule`。
2. `TreatmentPlan` 如无下游动作链，只能在图谱探索页作为知识节点展示，不得进入正式推荐区。
3. 治疗方案要进入 CDSS，必须拆成具体动作：药物、手术、检查、禁忌、适用条件、排除条件、证据。
4. 不允许为了清零而硬造关系，必须回到教材/指南原文补链。

本次处理：
- 标准主数据同名重复 39 组归并为 0。
- 缩写作为主名称归零。
- 18 条治疗方案无下游动作进入后续“CDSS 决策层治疗方案动作链增强”专项。

## 2026-07-16 13:01:34——入库后复测必须用中文口径，并把疾病层级纳入硬阻断

问题：
- 用户明确指出 `postcheck` 这类纯英文术语看不懂，不能作为日常交接语言。
- 早期质量闸门没有把疾病大类、疾病亚类、疾病/分型三层关系作为统一硬阻断，导致层级问题到前端展示时才暴露。
- 服务器还残留退役节点、已替换节点引用、同类型同名主数据重复，说明只修局部病种不够，必须进入通用入库后复测。

固化规则：
1. 对用户、SKILL、报告、交接文件统一使用“入库后复测”；英文机器字段只允许在脚本内部或兼容字段中出现。
2. 每个正式批次写入 Neo4j 后，必须执行 `公共执行层_kg_pipeline/批次入库后复测.py`。
3. 疾病层级 6 项硬闸门必须全部为 0：疾病无大类、疾病无亚类、疾病亚类无大类、疾病分型无父疾病、正式疾病同名重复、退役疾病节点残留。
4. 主数据质量 5 项硬闸门必须全部为 0：同病种同类型同名重复直连、诊断标准无明细、孤儿诊断标准、治疗方案无下游、已替换重复节点仍被引用。
5. 入库后复测未通过，不得写“已入库并通过”，不得转正式 CDSS，不得继续叠加新病种。

本次处理：
- 通用主数据质量闸门扩展为 11 项。
- 清理退役疾病节点 5 个、已替换节点残留引用 677 条、同类型同名主数据重复 82 组。
- 最终服务器主数据质量闸门和批次入库后复测总入口均通过，阻断项 0。

## 2026-07-16 12:06:56——疾病层级关系不能只看 Disease 是否存在，必须检查三层目录和分型闭环
问题：
- AMI 与 STEMI/NSTEMI 暴露出父子层级不清；全库进一步发现 19 个疾病目录断链、56 个孤立疾病分型、7 组同名疾病重复。
- 早期质量闸门偏重 required 字段、诊断标准明细、治疗下游关系，没有把“疾病大类 → 疾病亚类 → 疾病/分型”作为硬阻断。

固化规则：
1. 每个正式 Disease 必须至少能连到一个 DiseaseCategory 和一个 DiseaseSubcategory。
2. 每个正式 DiseaseSubcategory 必须能连到 DiseaseCategory。
3. 每个正式 DiseaseClassification 必须有父疾病；不能判定父疾病的分型不得留在正式分型层。
4. 同名 Disease 不允许长期共存；能确定标准节点时必须迁关系后物理删除旧重复节点。
5. 一病可多目录，例如肺动脉瓣狭窄可同时承接瓣膜病和先心病目录，但不能拆成两个同名疾病节点。
6. 后续所有新批次入库后复测必须输出：疾病无大类、疾病无亚类、亚类无大类、孤立分型、同名疾病、旧编码残留。

本次处理：
- 补齐 19 个目录断链、31 条父疾病分型关系。
- 36 个孤立分型退出正式层。
- 7 个旧同名疾病节点关系迁移后物理删除。
- 最终硬闸门 6 项全部为 0。

## 2026-07-16 11:05:00——术语字典存在不代表旧批次节点已归并，必须查服务器活跃节点

问题：
- `PCI` 已在手术同义词表中作为“经皮冠状动脉介入治疗”的别名存在。
- 但旧批次仍残留 disease-specific `PCI` 手术节点，并直接与 AMI/NSTEMI/UA 相连，导致前端展示缩写。

固化规则：
1. 术语字典只能说明“标准术语应该如何归一”，不能证明服务器旧节点已经归并。
2. 每次处理缩写/别名问题，必须查服务器活跃节点：`deprecated <> true` 且仍有业务关系。
3. 中文标准全称必须作为 `name/display_name/preferred_name`；英文缩写只能进入 `abbr` 或 `aliases`。
4. 旧缩写节点不得物理删除，先迁移关系，再标记 `deprecated=true` 和 `duplicate_replaced_by=标准节点code`。
5. 前端查询应默认过滤 `deprecated=true` 节点。

本次处理：
- 已将 AMI/NSTEMI/UA 下 3 个旧 `PCI` 节点关系迁移到“经皮冠状动脉介入治疗”标准节点。
- 旧 `PCI` 节点剩余关系为 0。
- 活跃 `Procedure.name=PCI` 节点为 0。

## 2026-07-16 00:25:00——Evidence 历史迁移必须分片执行，不能长事务全量跑

问题：
- 从 20 个节点扩大到 500–1000 个节点时，旧试点脚本逐个节点查询关系，耗时过长。
- 直接把扫描窗口放大到几千个候选，会造成批量状态查询变慢。

固化规则：
1. 历史 Evidence 迁移按 100 个重复节点一片执行。
2. 每片必须输出明细 CSV 和关系备份 CSV。
3. 每片完成后，重复节点剩余关系必须为 0。
4. 扩大迁移优先处理有原文文本、仍活跃、关系数低的 Evidence；空证据文本不得混入正式证据归并。
5. 统计必须用同口径：包含空证据文本一套，排除空证据文本一套，避免前后数字误判。

本次处理：
- 新增 `scripts/Evidence证据历史去重批量迁移_20260715.py`。
- 已完成 543 个重复 Evidence 节点迁移。
- 活跃可减少重复节点从 26,602 降至 26,059，下降 543。

## 2026-07-15 23:35:00——Evidence 复用不能停留在孤立脚本，必须进入 G2 硬闸门

问题：
- 只新增单独的 Evidence 检查脚本，容易在后续新批次执行时被遗漏，不能真正阻止重复证据节点继续产生。

固化规则：
1. Evidence 复用检查必须进入 `scripts/audit_graph_instance.py`，成为 G2 质量闸门的一部分。
2. 后续批次只要出现重复证据组、缺证据唯一键、非共享证据编号、共享编号不一致，G2 直接失败。
3. 单独脚本可以保留为专项排查工具，但不能替代 G2。

本次处理：
- 已把 Evidence 复用闸门接入本地 G2。
- 已同步到 `技能文档_skill_docs/附录F_质量闸门审计与Neo4j入库_quality_neo4j.md`。
- 已生成 `项目管理中心_project_management/42_Evidence复用闸门接入G2记录_20260715.md`。

## 2026-07-15 23:20:00——Evidence 迁移必须先迁关系再标记节点，不直接物理删除

问题：
- Evidence 被大量临床实体通过 `supported_by_evidence` 引用，直接删除或合并节点会造成证据链断裂。

固化规则：
1. 历史 Evidence 去重必须先迁移关系，再标记重复节点。
2. 重复节点先设置 `deprecated=true`、`duplicate_replaced_by=主证据code`，不得直接物理删除。
3. 每次迁移必须输出关系备份 CSV，便于回滚。
4. 迁移后必须复核：重复节点剩余关系数必须为 0，主证据关系必须存在。

本次处理：
- 已完成 20 个重复 Evidence 节点的小范围试点。
- 已迁移 15 条 `supported_by_evidence` 关系。
- 未物理删除节点。

## 2026-07-15 23:20:00——Evidence 统计必须区分全部节点和活跃节点

问题：
- 试点迁移后，重复 Evidence 节点仍保留在库中并标记 deprecated。如果统计脚本不排除 deprecated，会把已归并节点继续算作待治理重复，造成误判。

固化规则：
1. 全库历史统计可以包含全部 Evidence。
2. 后续治理进度统计必须使用“活跃 Evidence”口径：`deprecated <> true` 且 `duplicate_replaced_by IS NULL`。
3. 迁移清单默认只处理活跃 Evidence。

本次处理：
- `scripts/Evidence证据层统计与去重预演_20260715.py` 已改为输出活跃重复组。
- 试点后活跃重复证据分组为 7,061 组。

## 2026-07-15 23:10:00——Evidence 不能按疾病重复建节点

问题：
- 服务器存在 44,440 个 Evidence 证据节点，其中按“来源文件 + 页码 + 原文文本”判断有 7,081 组重复证据，理论可减少 26,622 个重复节点。
- 根因是同一份指南、同一页、同一段原文被按不同疾病后缀重复创建 Evidence。

固化规则：
1. Evidence 节点代表“证据原文来源”，不代表“某个疾病专属证据副本”。
2. 后续新批次必须先计算证据唯一键：来源文件名 + 页码 + 原文规范化文本。
3. 疾病、推荐、规则、阶段差异放在关系或推荐实体上，不再通过复制 Evidence 节点表达。
4. 服务器历史 Evidence 去重必须先做预演映射，确认后再迁移关系；禁止直接批量删除证据节点。

本次处理：
- 已生成 `项目管理中心_project_management/35_Evidence证据层瘦身与入库性能优化报告_20260715.md`。
- 已生成 `scripts/Evidence证据层统计与去重预演_20260715.py`。
- 已输出全量 7,081 组重复证据预演清单，未写库。

## 2026-07-15 23:10:00——Evidence 长文本必须摘要化和小批量入库

问题：
- 服务器 Evidence 中超过 5,000 字的节点 2,874 个，超过 10,000 字的节点 1,240 个。
- 五批次入库时，大段 Evidence 文本导致批量写入请求过大，出现 HTTP 500 或超时。

固化规则：
1. 医生端默认只展示推荐或规则直接关联的 1-3 条主证据，不展示疾病下全部 Evidence。
2. Neo4j 主查询路径只保留证据摘要、指南名称、页码、段落、推荐等级、证据等级、原文短摘录、原文哈希。
3. 超长原文保留在本地证据文件或文档库，图谱中保留定位和摘要，不直接作为医生端长列表展示。
4. Evidence 节点入库批量建议 20-50；证据关系入库批量建议 50-100；失败必须自动拆批并记录进度日志。

本次处理：
- 五批次入库脚本已验证小批量 Evidence 写入可通过。
- 已在 Evidence 专项报告中固化性能策略。

## 2026-07-15 19:02:00——疾病定义不能只补文本字段，必须形成 Definition 实体、定义明细和证据链

问题：

- 高血压旧批次 15 个 Disease 节点 `definition` 字段为空，虽然旧质检显示 required covered，但医生和前端无法直接看到疾病定义。
- 如果只把一句话写入 Disease.definition，没有 Definition / DefinitionComponent / Evidence 结构，后续前端无法下钻到原文依据，也不符合当前 Schema 的定义层设计。

固化规则：

1. 疾病定义必须同时满足两层：`Disease.definition` 便于快速展示，`Disease -> has_definition -> Definition -> has_definition_component -> DefinitionComponent` 便于结构化下钻。
2. `Definition` 与 `DefinitionComponent` 必须连接 Evidence；没有证据不得作为正式收口数据。
3. 本地 G2 必须检查：定义空值、Definition 无明细、Definition/DefinitionComponent 无证据、旧编码泄漏。

本次处理：

- 高血压正式收口本地生成 15 个 Disease.definition、15 个 Definition、45 个 DefinitionComponent、120 条关系。
- 本地 G2 阻断项 0；尚未写 Neo4j，待用户确认入库。

## 2026-07-15 19:02:00——高血压 HT/HTN 双编码混杂必须先定主编码再收口

问题：

- 旧骨架和 7/14 验收中同时存在 `DIS-CARD-HT-*` 与 `DIS-CARD-HTN-*`，会造成疾病数量虚高、前端展示重复、后续病种计划判断失真。

固化规则：

1. 高血压正式主线统一采用 `DIS-CARD-HT-*`。
2. `DIS-CARD-HTN-*` 只作为历史兼容映射，不允许新批次继续生成。
3. 急症和亚急症必须拆分为 `DIS-CARD-HT-EMERGENCY` 与 `DIS-CARD-HT-URGENCY`，不得再用旧合并编码。
4. 主动脉缩窄、嗜铬细胞瘤等不直接作为高血压主疾病闭环，按继发性高血压病因、鉴别或其他专病处理。

本次处理：

- 已生成 `高血压旧HTN编码兼容映射_20260715.csv`，并在本轮 delta 中阻断 `DIS-CARD-HTN-*` 泄漏。

## 2026-07-15 15:55:00｜本地 G2 漏检“诊断标准必须下钻明细”

问题：

- 起搏治疗相关疾病批次本地 G2 显示通过，但入库复核前发现 9 个诊断标准中有 8 个缺少 `has_diagnostic_component` 下钻明细。
- 如果只看“疾病有诊断标准节点”，前端会再次出现“诊断标准只有标题，没有下一级内容”的旧问题。

处理：

- 已为房室传导阻滞、一度房室传导阻滞、三度房室传导阻滞、束支传导阻滞、缓慢性心律失常、窦房传导阻滞、窦性心动过缓、窦房结功能障碍补齐诊断标准明细。
- 已绑定教材/指南证据，并二次入库服务器 Neo4j。
- 起搏治疗本批次服务器 postcheck 已验证：诊断标准无明细=0。

复用规则：

- 所有新批次入库前必须检查：`Disease -> DiagnosisCriteria -> DiagnosisCriteriaComponent` 是否完整。
- 不能只要求有 `DiagnosisCriteria` 节点；没有明细的诊断标准视为硬阻断。
- 该检查必须进入通用 kg_pipeline 和 SKILL 入库前/入库后双重闸门。

## 2026-07-15 14:05:00｜错误指纹：本地 G1 通过不等于服务器 postcheck 通过

### 触发场景

肺动脉高压批次本地 G1 审计通过后，服务器入库后复测发现 7 个诊断标准没有下钻明细，以及 25 个 ClinicalRule 存在跨章节污染风险。本地 G1 主要校验结构和字段完整性，不足以发现“诊断标准标题空壳”和“相邻章节语义污染”。

### 根因

本地审计口径没有把服务器实际图谱中的 postcheck 闸门全部纳入；同时 ClinicalRule 抽取仍存在“只要段落含目标疾病词就提升为规则”的问题。

### 固化规则

1. 所有正式入库必须执行两层闸门：本地 G1/G2 + 服务器 postcheck。
2. 只有服务器 postcheck 阻断项为 0，才能标记为“已入库并通过”。
3. postcheck 必须包含：非 KGNode、重复同名、疾病定义缺失、诊断标准无明细、鉴别诊断无明细、治疗方案无下游动作、药物类别无具体药物、推荐关系核心字段缺失、技术名节点、跨章节污染规则。

### 本次处理

已删除肺动脉高压污染规则节点 25 个和关系 143 条，补齐诊断标准明细 7 条，重新入库后本批次服务器 postcheck 阻断项归零。

### 标准文档同步

已于 2026-07-15 14:34:29 同步到：

1. `专科知识图谱Schema标准.md` V1.16。
2. `AI自动化工具-文献指南解析.md` V2.2。

## 2026-07-15 14:05:00｜错误指纹：含目标词的相邻章节不能直接提升为本病 ClinicalRule

### 触发场景

肺动脉高压批次中，部分教材或指南段落虽然包含“肺动脉高压”字样，但正文实际属于房颤、房扑、室上速、电复律、冠心病、心肌梗死、瓣膜病或慢性肺心病等相邻章节。抽取时如果只按关键词命中，会把相邻章节内容误提升为本病 ClinicalRule。

### 根因

规则抽取缺少“章节主题一致性”判断，只做了疾病关键词命中。

### 固化规则

1. ClinicalRule 必须同时满足：疾病范围命中、段落主题命中、来源章节匹配。
2. 仅出现目标疾病名称，不代表段落可归属该疾病。
3. 对高风险相邻章节关键词必须做排除检查，例如房颤、房扑、室上速、电复律、冠心病、心肌梗死等。
4. 相邻章节内容只能作为鉴别、并发或背景线索，不能直接作为本病 CDSS 推荐规则。

### 本次处理

已保留可复用收口脚本：`scripts/肺动脉高压入库后质量收口_20260715.py`。

### 标准文档同步

已于 2026-07-15 14:34:29 同步到 Schema 和解析技能文档，后续批次必须执行章节主题一致性校验。

## 2026-07-15 13:45:00｜错误指纹：英文缩写进入图谱后，必须同步沉淀到术语字典和别名字段

### 触发场景

用户追问 PEA/BPA 这类英文缩写是否有翻译术语字典和图谱别名对照。核查发现：肺动脉高压本地图谱节点中已经存在 aliases，但共享 `术语字典/6_手术同义词表.yaml` 尚未沉淀 PEA/BPA。

### 根因

本地批次修复只补了图谱节点别名，没有同步回写共享术语字典，导致下一批次、前端展示、后端查询或证据匹配仍可能重复踩坑。

### 固化规则

1. 任何英文缩写进入图谱后，必须同步检查并更新对应术语字典。
2. 中文标准全称放 `name/display_name/canonical`，英文缩写和英文全称放 `aliases`。
3. PEA/BPA 属于手术/操作类术语，应维护在 `6_手术同义词表.yaml`，不得放入药物字典。
4. 术语字典和图谱节点必须双向一致：字典用于后续抽取，图谱 aliases 用于查询、展示和证据命中。
5. 如发现术语字典文件乱码，不能继续追加乱码内容，必须先恢复为可读中文再维护。

## 2026-07-15 13:30:08｜错误指纹：短英文缩写会误命中证据段，治疗关系必须优先匹配中文目标词和明确别名

### 触发场景

肺动脉高压批次中，PEA、BPA 等短英文缩写会在英文机构名、普通英文单词片段或无关段落中误命中，导致治疗动作关系挂到错误证据段，目标实体/证据命中闸门失败。

### 根因

证据选择逻辑只按短英文缩写优先匹配，没有优先使用中文标准名和高置信别名；同时部分治疗关系已存在时未覆盖旧证据，导致错误证据保留。

### 固化规则

1. 治疗关系证据选择顺序必须为：疾病范围内中文标准名 > 中文别名 > 英文全称 > 短英文缩写。
2. 短英文缩写只能作为辅助匹配，不得作为首选证据定位依据。
3. 修复脚本必须同时处理“已存在错误关系”，不能只新增关系。
4. 若目标证据文本不含目标实体标准名或别名，禁止通过质量闸门。

## 2026-07-15 13:30:08｜错误指纹：审计脚本必须兼容多别名和多缩写字段

### 触发场景

肺动脉高压批次修复后，节点 `abbr` 字段为列表时，审计函数 `_mentions` 报错：`list object has no attribute isascii`。

### 根因

审计函数只把 `aliases` 当作多值字段处理，却把 `abbr` 当作单一字符串处理；但药品、检查、手术等实体可能存在多个英文缩写或英文名。

### 固化规则

1. `aliases` 和 `abbr` 都必须按多值字段处理。
2. 审计函数不得假设 `abbr` 一定是字符串。
3. 新增药物、检查、手术、规则节点时，中文标准名放 `name/display_name`，英文缩写或英文名放 `abbr/aliases`。
4. 该类问题属于公共执行层缺陷，修复后必须保留在脚本和踩坑日志中。

## 2026-07-15 11:54:27｜错误指纹：本地质量通过后，服务器合并入库仍可能暴露主数据重复和诊断明细缺口

### 触发场景
瓣膜病正式批次本地质量闸门已通过，用户确认入库后，服务器 Neo4j 幂等合并写入成功；但入库后 postcheck 首轮失败，阻断项为：诊断标准无明细 9 条、同疾病同类型同名重复 33 组、已替换重复节点仍被引用 2 条。

### 根因
本地批次只检查本批次内部质量，不能完全发现“新批次实体与服务器既有标准主节点合并后”的重复主数据、旧替换节点残留引用和诊断标准明细缺口。服务器合并后的真实图谱状态必须以入库后 postcheck 为准。

### 防复发规则
1. “本地 G1/G2 通过”只能说明候选包可入库，不能直接等同于服务器通过。
2. 所有写入 Neo4j 的批次，必须执行 `公共执行层_kg_pipeline/批次入库后复测_postcheck.py`。
3. 服务器 postcheck 未通过时，不得标记“已入库并通过”，必须先修复或明确阻断。
4. 新批次入库前优先复用服务器已有标准主节点；入库后发现重复时，必须迁移关系到标准主节点，不得只删除节点。

### 本轮处理
- 补齐瓣膜病 9 个 `DiagnosisCriteriaComponent` 诊断标准明细。
- 迁移并归并 33 组同疾病、同类型、同名重复实体关系。
- 修复 2 条旧重复节点残留引用。
- 复测通过：同疾病同类型同名重复=0，诊断标准无明细=0，孤儿诊断标准=0，治疗方案无下游动作=0，已替换重复节点仍被引用=0。

## 2026-07-15 09:40:00｜错误指纹：正式解析不能只靠旧术语表，必须读取批次范围表中的疾病锚点

### 触发场景
瓣膜病正式解析初始运行时，指南证据抽取结果为 0。进一步排查发现，当前 G0 预检已经把疾病范围写入 `scope_taxonomy.csv`，但旧证据抽取脚本仍只读取 `controlled_vocabulary.csv` 中的 Disease 行；本批次术语表主要是操作/药物等术语，导致疾病锚点为空。

### 根因
公共执行层升级后，批次范围来源已经发生变化，但旧抽取脚本没有同步适配。技能文档和 Schema 升级不等于所有历史脚本自动升级。

### 防复发规则
1. 新批次证据抽取必须合并读取 `controlled_vocabulary.csv` 和 `scope_taxonomy.csv`。
2. 如果证据数为 0，不得直接判断“资料不足”，必须先检查疾病锚点来源。
3. G0 输出结构变化后，G1/G2 脚本必须有回归测试覆盖。

### 本轮处理
- 已修复 `scripts/extract_guideline_evidence.py`。
- 已新增回归测试：从 `scope_taxonomy.csv` 读取疾病锚点。

## 2026-07-15 09:40:00｜错误指纹：槽位容器不能按合成名称做证据命中；应按“疾病锚点 + 槽位关键词”审计

### 触发场景
瓣膜病质量审计中，`主动脉瓣反流检查`、`二尖瓣狭窄体征`、`多瓣膜病诊断标准` 等槽位容器被判定目标名未命中证据原文。

### 根因
这些名称是图谱为了组织临床维度而生成的容器名，教材/指南原文通常不会逐字出现“某病检查”这种合成短语。旧审计把所有目标实体都按“目标名/别名必须出现在证据原文”处理，导致误报。

### 防复发规则
1. 具体临床实体继续按目标名/别名命中。
2. 诊断标准、检查、体征、病因、治疗方案等槽位容器改用“疾病名或别名 + 维度关键词”命中。
3. 审计脚本必须输出失败明细 CSV，不能只给数量。

### 本轮处理
- 已修复 `scripts/audit_graph_instance.py`。
- 已新增 `target_match_failure_register.csv` 输出。
- 瓣膜病最终目标实体/槽位证据命中率为 100%。

## 2026-07-15 09:40:00｜错误指纹：病因段不能因出现容量负荷等词被误判为病理生理

### 触发场景
瓣膜病解析中，主动脉瓣反流的“病因”段因为出现容量负荷、反流等词，被误挂到病理生理槽位；主动脉瓣狭窄、风湿性心脏瓣膜病也被误带入同一段证据。

### 根因
旧分类规则主要按关键词命中，没有优先识别章节标题。“病因”段中可以出现血流动力学后果，但这不代表该段主语义就是病理生理。

### 防复发规则
1. 教材/指南原文存在明确标题时，标题优先级高于正文关键词。
2. `【病因】`、`【病因和发病机制】` 优先归病因；`【病理生理】` 才优先归病理生理。
3. 对同一段落跨病种挂载时，必须检查疾病锚点是否成立。

### 本轮处理
- 已修复 `_pathway_element` 标题优先级。
- 已新增回归测试：病因标题优先于容量负荷等病理生理关键词。

## 2026-07-14 20:50:00｜错误指纹修正：跨疾病证据不是直接删除问题，而是证据范围过滤问题

### 触发场景
心衰、心律失常、高血压复测前，曾把“共享节点上存在其他疾病证据”视为硬缺陷。进一步分析发现，很多共享节点上的其他疾病证据对原疾病是正确的，不能为了当前疾病页面而删除或全局阻断。

### 根因
图谱中共享治疗/规则节点天然会连接多个疾病证据。真正的问题不是“存在其他疾病证据”，而是前端或后端查询时没有按当前疾病范围过滤。

### 防复发规则
1. 不得因为某个共享节点有外部疾病证据就删除证据关系。
2. Evidence/Guideline 的适用疾病范围必须沉淀到 `supported_by_evidence.disease_code`。
3. CDSS 查询证据必须用 `$allowed_disease_codes` 过滤。
4. 大类验收硬闸门检查“证据是否缺少疾病范围”，而不是把所有外部疾病证据都算成缺陷。
5. 前端不得展示推荐节点连接的全部证据。

### 本轮处理
- 回填 98,132 条证据关系疾病范围字段。
- 三类复测后 `CDSS推荐证据缺少疾病范围=0`。
- Trae 和 Cypher 查询文档已同步新规则。

## 2026-07-14 20:14:00｜错误指纹：大类验收不能只看“有证据”，必须看证据是否属于当前疾病范围

### 触发场景
心力衰竭、心律失常、高血压大类验收时，常规结构硬闸门均为 0，但进一步检查发现共享 CDSS 节点挂载了大量其他疾病的证据。例如通用“抗凝治疗”“血运重建”“降压治疗”等节点被多个疾病复用后，证据链可能从冠心病、心肌病或其他章节串入当前疾病。

### 根因
1. 早期检查只判断推荐节点是否连接 Evidence/Guideline，没有校验证据节点或证据关系的 `disease_code` 是否属于当前大类。
2. 通用治疗/规则节点被复用时，如果证据边没有疾病范围约束，会在前端和 CDSS 查询里形成“一箩筐证据”。
3. 大类筛选如果使用疾病名称模糊包含，会把非本大类疾病误纳入统计范围；本轮高血压初筛曾被扩大，已改为编码前缀口径。

### 防复发规则
1. 大类验收范围优先使用疾病编码前缀，不得把宽泛疾病名称作为主筛选条件。
2. CDSS 正式推荐必须满足：当前疾病/大类 + 推荐节点 + 证据范围一致。
3. 共享 TreatmentPlan / ClinicalRule / RecommendationStatement 不得无差别继承其他疾病证据。
4. `CDSS推荐证据疾病范围不一致` 必须作为硬闸门进入后续所有大类交付基线验收。
5. 发现大量证据范围串台时，不得粗暴删除共享节点或证据关系；应先做疾病范围隔离治理。

### 本轮处理
- 已把 `CDSS推荐证据疾病范围不一致` 加入大类验收脚本硬闸门。
- 已重新生成心力衰竭、心律失常、高血压三类验收包。
- 本轮未写 Neo4j，原因是问题属于共享证据范围隔离，直接删关系风险高。

## 2026-07-14 19:48:00｜执行规则：大类验收不等于必须写库；无缺口时只留基线报告和防串台复核

### 触发场景
心肌病大类验收时，常规5项硬质量全部为0，额外的共享推荐节点证据疾病标记复核也为0。

### 风险点
如果形成“每轮必须写库”的惯性，会为了形式补链或阻断，反而制造新污染。大类验收的目标是确认质量和交付状态，不是强行修改数据库。

### 防复发规则
1. 大类验收先只读复核；只有发现真实缺口才写 Neo4j。
2. 硬质量为0时，不做形式化补链。
3. 证据链验收必须额外检查共享推荐节点是否存在跨疾病串台风险。
4. 低分疾病代表覆盖深度不足，不等于硬质量失败；应进入后续文献补强，而不是临时凑节点。
5. 单个样板专病只用于技术联调，不得替代大类交付判断。

### 本轮处理
- 心肌病大类 12 个疾病/分型完成交付基线验收。
- 未写库，未新增或删除 Neo4j 数据。
- 生成心肌病大类验收报告和证据防串台复核文件。

## 2026-07-14 19:24:00｜错误指纹：共享推荐节点的证据链不能跨疾病串台；弱证据不得硬补

### 触发场景
冠心病大类验收时发现 20 条 CDSS 推荐节点缺直接证据链。初始候选匹配虽然 20 条都能找到关键词命中证据，但抽查发现部分候选来自脑出血、高血压、肿瘤房颤、瓣膜病等场景，不能直接作为冠心病对应推荐的正式证据。

### 根因
1. 早期证据链校验只看推荐节点是否连到 Evidence/Guideline，未区分该证据是否属于当前疾病。
2. 共享治疗节点被多个疾病复用时，如果不做“疾病-推荐-证据”三者绑定，容易出现一个疾病的证据被另一个疾病误用。
3. 单纯关键词匹配会把同名或近义场景误认为强证据，例如“降压治疗”可能来自高血压章节，不等于AMI治疗推荐。

### 防复发规则
1. CDSS正式推荐必须满足：当前疾病 + 当前推荐节点 + 对应 Evidence/Guideline 三者成立。
2. 证据补链前必须抽查首选证据的疾病、动作和场景；只命中关键词不足以写入正式证据链。
3. 对弱证据或场景错位，不删除节点；应设置关系级 `formal_cdss_ready=false`，并写明阻断原因。
4. AMI/STEMI 只能作为冠心病大类中的技术样板，不能代替冠心病大类验收口径。
5. 后续大类验收脚本必须避免共享节点证据串台。

### 本轮处理
- 15 条推荐节点补齐 `supported_by_evidence` 证据链。
- 5 条弱证据/错位场景关系被挡在正式CDSS外。
- 冠心病大类复测：5项硬质量缺口全部为0。

## 2026-07-14 15:51:00｜错误指纹：SKILL升级后旧脚本仍在，不能默认继续使用旧入口

### 问题表现
- SKILL和Schema已经升级，但 `scripts/` 目录仍保留6月、7月早期一次性脚本。
- 部分旧脚本存在编码不稳定、旧连接写法、职责不清等问题。
- 如果新病种继续直接调用旧脚本，会绕过公共质量闸门，导致同类问题反复出现。

### 根因
SKILL升级只定义“未来怎么做”，不会自动删除、重构或替换历史脚本。历史脚本必须通过脚本资产台账分级管理，并由公共执行层逐步替代。

### 复用规则
1. 新批次默认只使用 `公共执行层_kg_pipeline` 入口。
2. 旧脚本仅作为历史追溯和问题排查资料。
3. 若旧脚本要重新启用，必须先迁移到公共执行层并补测试。
4. 写入 Neo4j 后统一执行 `批次入库后复测_postcheck.py`。

### 本次固化
- 新增：`公共执行层_kg_pipeline/批次入库后复测_postcheck.py`
- 新增：`tests/test_batch_postimport_postcheck.py`
- 降级：`scripts/run_server_global_cdss_safety_audit.py` 不再作为新批次默认入口。

## 2026-07-14 15:27:00｜错误指纹：重复节点归并后，所有疾病/路径/规则引用必须迁移到标准主节点

### 触发场景
主数据质量闸门首次执行时发现，`缺血性心肌病治疗方案` 仍通过 `includes_procedure` 指向旧重复操作节点 `PROC-CARD-CADREM-ICM-9360DBD2DE（心脏移植）`，虽然该节点已经标记为被标准主节点 `PROC-HEART-TRANSPLANT（心脏移植）` 替换。

### 根因
上一轮主数据归并已经处理了大部分疾病直连和下游关系，但仍存在个别路径/治疗方案关系指向旧重复节点。说明“重复节点被打标”不等于“全部引用已迁移”。

### 防复发规则
1. 重复节点一旦标记 `duplicate_replaced_by`，疾病、路径、规则、推荐、治疗方案不得继续指向该节点。
2. 入库后必须查“已替换重复节点仍被引用”。
3. 如发现残留引用，优先迁移关系到标准主节点；只有零关系孤儿节点才允许精确删除。
4. 该检查已固化到 `公共执行层_kg_pipeline/主数据质量闸门_master_data_gate.py`。

### 本轮处理
- 迁移 1 条 `includes_procedure` 关系。
- 修复后主数据质量闸门 5 项全部为 0。

## 2026-07-14 15:05:00｜错误指纹：教材骨架与专病批次双挂会制造同病种同类型同名主数据重复

### 触发场景
全库复核发现，历史教材骨架、冠心病/心肌病/心律失常等专病批次同时把同一疾病下的同名症状、体征、检查、检验、药物、治疗方案、诊断标准等挂到疾病，造成前端展示重复、规则推理入口不稳定。

### 根因
早期批次没有统一标准主节点选择和主数据归并规则。教材骨架节点、专病指南节点、后续补丁节点并存时，只迁移了部分疾病直连关系，未同步迁移下游关系、证据关系和路径/规则引用。

### 防复发规则
1. 新批次入库前必须查：同一疾病 + 同一 `entityType` + 同一 `name` 是否已有标准主节点。
2. 如果已有标准主节点，优先复用标准主节点，只新增疾病关系、证据关系、规则关系，不重复创建同名主节点。
3. `Evidence`、`Guideline`、`RecommendationStatement`、`ClinicalRule`、`ClinicalPathway` 不参与名称归并，避免丢失证据语义。
4. 空壳 `TreatmentPlan` 不得进入正式 CDSS；如果已有同名标准主节点且有下游动作/证据，应迁移引用后清理空壳。
5. 零关系孤儿节点可精确删除；有关系的节点必须先迁移或阻断，不得 `DETACH DELETE`。
6. 每轮治理必须输出候选清单、阻断清单、执行明细、最终硬闸门复核，并同步批次台账。

### 本轮处理
- 148 组同病种同类型同名重复直连已归并。
- 149 个重复节点关系已迁移并打标。
- 4 个零关系孤儿诊断标准已精确删除。
- 1 个空壳“复律治疗”已并入标准主节点，3 条路径/规则关系已迁移。
- 最终硬闸门：重复直连、非 KGNode、标签不一致、诊断标准无明细、鉴别诊断无明细、治疗方案无下游均为 0。

## 2026-07-14 14:26:00｜错误指纹：传导阻滞类治疗方案不能只有疾病级空方案，必须补观察随访、诱因纠正、起搏治疗等可执行子方案

### 触发场景
心衰、心律失常、高血压三范围总精修时，第一轮规则证据链和治疗方案补链后仍有 4 个传导阻滞相关 `TreatmentPlan` 被硬闸门阻断：一度房室传导阻滞、二度房室传导阻滞、束支传导阻滞、窦房传导阻滞的疾病级治疗方案只有标题或证据，缺少可执行下游。

### 根因
教材骨架和历史抽取把“传导阻滞治疗方案”作为疾病级总标题写入，但没有同步拆成临床可执行动作。对于传导阻滞类疾病，真正可用的 CDSS 结构应包含：观察随访、诱因纠正、起搏治疗评估，以及临时起搏/永久起搏器植入等具体操作。

### 防复发规则
1. 传导阻滞、缓慢性心律失常、心衰、冠心病等疾病的顶层治疗方案不得只有标题。
2. 疾病级 `TreatmentPlan` 必须至少下接一个可执行子方案或操作：如观察随访、诱因纠正、起搏治疗、临时起搏、永久起搏器植入。
3. `has_clinical_pathway` 只表示进入路径，不等于治疗方案已有动作。
4. 转 `formal_cdss_ready=true` 前，必须检查 `TreatmentPlan` 下游动作缺口为 0。
5. 如果某疾病确实只有观察随访，也要显式建模为 `观察随访` 子方案，不能保留空壳。

### 本轮处理
- 4 个传导阻滞类阻断治疗方案已补齐。
- 新增/复用 8 条治疗组件关系。
- 起搏治疗已连接临时起搏和永久起搏器植入。
- 心衰、心律失常、高血压 65 个疾病全部通过本轮硬闸门并转正式。

## 2026-07-14 13:54:00｜错误指纹：疾病级治疗方案不能只挂路径；同病种同类型同名直连必须清零后才能转正式

### 触发场景
心肌病总精修扩展时，规则证据链补齐后，转正式脚本发现：部分顶层 `TreatmentPlan` 只挂 `has_clinical_pathway` 和证据，前端/规则引擎若只看治疗方案直连会看不到具体动作；另有 24 组同病种同类型同名重复直连。

### 根因
早期批次把“疾病总治疗方案”“路径”“具体治疗动作”分层不稳定；后续补路径时没有同步给顶层治疗方案建立可执行直连。历史精修还同时保留了通用节点和专病批次节点，造成同病种重复展示。

### 防复发规则
1. 顶层 `TreatmentPlan` 必须至少直连 `includes_medication`、`includes_procedure` 或 `has_treatment_component`。
2. `has_clinical_pathway` 只表示进入路径，不等于治疗方案已有具体动作。
3. 病因、分层、评估句不能放入 `TreatmentPlan`。
4. 转 `formal_cdss_ready=true` 前，必须检查同病种同类型同名重复直连=0。
5. 重复治理优先迁移疾病直连关系和证据/诊断明细关系；不做粗暴 `DETACH DELETE`。

### 本轮处理
- 心肌病 28 条 `ClinicalRule` 已补 `supported_by_evidence`。
- 10 个顶层 `TreatmentPlan` 已补直连动作/子方案。
- 心肌炎 1 个误归治疗方案的病因句已改为 `Etiology`。
- 24 组同名重复直连已治理为 0。
- 心肌病 13 个疾病节点、103 个核心节点已转 `formal_cdss_ready=true`。

## 2026-07-14 10:35:05｜错误指纹：药物/检验缩写不得作为主名称，缩写必须进入 aliases

### 触发场景
总精修批次复核时发现 `ACEI` 作为药物主名称；用户指出应记录真实药品/药物类别完整名称，缩写应放别名。同步检查发现 `CK-MB` 属于检验项目/指标，不是药物。

### 根因
早期抽取和回填时只做了实体类型识别，没有强制执行“医生可见主名称”规则；药物、检验、检查指标的缩写处理策略没有纳入硬闸门。

### 防复发规则
1. `Medication.name/preferred_name/display_name` 必须是中文或通用完整名称；英文缩写、中文简称、英文全称只能放 `aliases`。
2. `LabTest/ExamIndicator` 同理；允许主名写“中文全称（国际缩写）”，如“肌酸激酶同工酶（CK-MB）”。
3. `CK-MB`、`BNP`、`NT-proBNP`、`D-二聚体` 等不能误归入 `Medication`。
4. G1/postcheck 必须检查：药物缩写作为主名=0，检验缩写误归药物=0。
5. 新增批次目录必须使用“日期_中文主题”，不得再使用全英文长目录。
6. 任何 Neo4j 命名/alias 修复完成后，必须同步回写 `术语字典/*.yaml`、`术语维护记录.md`、`terminology_alias_ledger.csv`；只改数据库不改字典视为未闭环。

### 本轮处理
- `ACEI` 改为 `血管紧张素转换酶抑制剂`，`ACEI/ACE抑制剂` 等进入 aliases。
- `GPⅡb/Ⅲa受体拮抗剂` 改为 `糖蛋白Ⅱb/Ⅲa受体拮抗剂`，原写法进入 aliases。
- 6 个 CK-MB 相关 `LabTest` 统一主名为“肌酸激酶同工酶（CK-MB）”。
- 11 个缺类型标签节点已补齐。
- 已回写 `术语字典/4_药物同义词表.yaml`、`术语字典/5_检查同义词表.yaml`、`术语字典/术语维护记录.md`。
- 已回写 `心血管内科文献集合/00_术语别名台账_terminology_alias_ledger/terminology_alias_ledger.csv`，新增 15 条别名记录。
- 最终硬闸门通过。

## 2026-07-14 10:02:00｜错误指纹：真实鉴别对象不能只有对象节点，必须补鉴别规则明细

### 问题
历史治理后保留了 14 个真实 `DifferentialDiagnosis` 对象，但对象下面缺少 `has_differential_point -> ClinicalRule`，前端展示时只能看到鉴别对象名称，看不到如何鉴别。

### 根因
早期把“鉴别诊断对象”和“鉴别诊断规则”混在一起处理，后来清理类型时虽然保留了真实对象，但没有同步补充规则明细；另有一批自动候选仅靠关键词匹配，存在证据泛化和 OCR 噪声。

### 复用规则
1. `DifferentialDiagnosis` 只表示“需要鉴别的对象”。
2. 具体鉴别逻辑必须落到 `ClinicalRule`，通过 `has_differential_point` 连接。
3. `ClinicalRule` 必须有 `rule_text/original_text` 和 `supported_by_evidence`。
4. 自动候选必须做证据语义核验；不允许为了数量好看写入弱证据。
5. 历史短句型规则如没有完整证据，必须 `blocked + formal_cdss_ready=no`，不得进入正式 CDSS。

### 本次处理
- 14 个真实鉴别对象全部补齐规则明细。
- 52 条旧自动候选拒绝入库。
- 496 条旧 `rule_logic` 兼容回填 `rule_text`。
- 180 条历史短句型规则隔离为 blocked。

## 2026-07-14 08:32:00｜证据来源类型要分清：教材证据不能伪装成指南

## 2026-07-14 09:20:46｜错误指纹：句子型鉴别要点不得标为 DifferentialDiagnosis

### 触发场景
早期抽取把“P波形态与窦性P波不同”“可通过病史、TTE、CMR、冠脉造影鉴别”等句子型鉴别要点标为 `DifferentialDiagnosis`，导致前端把鉴别要点当成鉴别疾病展示，且硬闸门提示鉴别诊断无明细。

### 根因
实体粒度混淆：`DifferentialDiagnosis` 应表示需要鉴别的疾病/状态；鉴别要点、排除逻辑、鉴别规则应落到 `ClinicalRule`，并由 `has_differential_point` 连接。

### 防复发规则
1. 短疾病/状态名可作为 `DifferentialDiagnosis`。
2. 含“鉴别、排除、根据、提示、与、不同、表现”等句子型内容，一律不得作为 `DifferentialDiagnosis`。
3. 句子型鉴别内容进入 `ClinicalRule(rule_category=differential_point)`。
4. `differentiates_from` 的目标必须是 `DifferentialDiagnosis`，不得指向规则句子。

### 本轮处理
- 批次：`BATCH-CARD-HIST-TYPE-CLEAN-20260714-001`
- 改型节点：146 个
- 转换关系：160 条
- 诊断标准误分型清零；鉴别诊断剩余 14 个真实对象待补规则。


## 2026-07-14 08:53:44｜错误指纹：Evidence 名称不能替代证据正文语义校验

### 触发场景
VTE 历史骨架中出现大量 `静脉血栓症-xxx-全书回捞证据` 形式的 Evidence 名称，但证据正文实际来自冠心病、高血压、心衰、感染性心内膜炎等章节，导致 VTE 下挂载错误证据和错误实体。

### 根因
抽取/回填时把生成的 Evidence 名称当成了证据语义依据，没有强制校验 `evidence_text`、来源章节、来源疾病主体是否包含当前疾病或其标准同义词（VTE/PTE/DVT/静脉血栓/肺栓塞/深静脉血栓等）。

### 防复发规则
1. 任何疾病到实体/证据关系，不能只看 Evidence 节点名称。
2. `evidence_text/source_section_path/source_file` 至少一个字段必须能支撑当前疾病语义主体；否则进入候选/阻断，不得直接入库。
3. 临床上可能相关但证据不足的项，保留为 `review_ambiguous`，后续补原文证据后重建，不得粗暴删除。
4. 后续批次审计必须输出“疾病语义主体命中”字段。

### 本轮处理
- 批次：`BATCH-CARD-VTE-CLEAN-20260714-001`
- 删除 VTE 错误关系：350 条
- VTE 专项闸门：通过
- 全库正式 CDSS 硬闸门：通过


### 典型表现
全量 RecommendationStatement 旧审计口径要求每条推荐都必须有 `based_on_guideline`。但 STEMI 教材来源推荐已经有 `supported_by_evidence` 和 `recommends_action`，只是证据来源为《内科学（第10版）》，不是指南。

### 根因
审计规则把“指南推荐”和“教材骨架推荐”混为同一类，导致教材证据推荐被误判为缺 guideline。

### 防复发规则
1. `RecommendationStatement` 必须有证据和动作。
2. 若证据 `source_type=guideline`，必须有 `based_on_guideline`。
3. 若证据 `source_type=authoritative_textbook`，不强制 `based_on_guideline`，但必须保留 `supported_by_evidence`。
4. 不允许为了清零把教材节点伪装成 Guideline。
5. 已有 `derived_from -> Evidence` 的推荐，应同步补 `supported_by_evidence -> Evidence`，避免前端和审计读取不到主证据。

### 本轮固化
`BATCH-CARD-RS-GAP-20260714-002` 已将高血压 50 条推荐的派生证据补为主证据关系，并补齐 STEMI 溶栓阻断推荐的阻断规则动作；合理审计口径缺口归零。

## 2026-07-14 08:20:00｜共享诊断标准不能只有标题，必须能下钻到诊断明细

### 典型表现
传导阻滞相关 6 个疾病均挂到同一个 `DiagnosisCriteria`，前端只能看到“心电图传导阻滞诊断依据”标题，无法看到一度、二度Ⅰ型、二度Ⅱ型、高度、三度/完全性房室传导阻滞及窦房传导阻滞的具体判断内容。

### 根因
早期抽取把诊断标准抽成标题型节点，没有同步生成 `DiagnosisCriteriaComponent`，导致多个疾病引用同一空壳标准时，缺口会被放大统计。

### 防复发规则
1. `DiagnosisCriteria` 不得只有标题；必须至少有 1 条 `has_diagnostic_component`。
2. 如果一个诊断标准被多个疾病共享，优先补共享父节点的组件，避免逐病种重复补。
3. 组件必须包含 `component_text`、`structured_logic`、`source_evidence_code`，不能只有规则名称。
4. 后续前端必须下钻展示组件，不要只展示父诊断标准标题。

### 本轮固化
`BATCH-CARD-DXC-AVB-COMP-20260714-001` 已补齐 8 个组件并导入 Neo4j；全库 `diagnosis_criteria_without_component` 从 6 降为 0。该问题归并到既有指纹 `EFP-20260714-013`，不新建重复错误指纹。

## 2026-07-13 23:27:54｜检验指标补链不能跨疾病套用同名证据，审计总数不能用 LIMIT 样本代替

### 典型表现
高血压批次中 51 条 required LabTest 缺口实际是共享检验项目缺少指标层；血培养部分关系已有原文，可以安全转成 Evidence；但 D-二聚体虽然库里有 AMI 相关证据，不能直接拿来补 VTE 或急性心包炎。

另一个问题是旧审计脚本把 `RecommendationStatement` 缺核心链路查询限制在 `LIMIT 100`，导致之前报告显示 100 条，实际全量复核为 344 条。

### 防复发规则
1. 同名 `LabTest` 只能共享“通用指标层”关系，不能共享“疾病场景结论”。
2. 自动补链必须满足：目标 LabTest 有非空证据，或当前疾病-检验关系有非空且语义匹配的原文证据。
3. 没有当前疾病场景原文证据时，进入 blocker，不跨病种套证据。
4. 全库质量体检必须区分“总数 count”和“样本 sample”，不得把 `LIMIT 100` 结果写成总缺口。
5. 下一轮治理要重写旧审计脚本，参数化输出目录，并修复历史编码污染。

### 本轮固化
`BATCH-CARD-HT-LABIND-20260713-001` 已将 51 条高血压缺口和 3 条血培养缺口修复入库；2 条 D-二聚体保留阻断。

# 全局复利与踩坑日志

用途：只记录会复发、会影响后续批次质量、需要沉淀为规则或自动化校验的问题。历史长日志已归档到 `日志归档_log_archive/`。

## 2026-07-13 18:20:00｜关系有 evidence_text 不等于能支撑当前疾病

### 典型表现

全局检验指标补证后剩余 `急性心肌梗死 -> 血培养`、`心力衰竭 -> 血培养` 两条关系。表面看关系属性里有 `evidence_text`，但进一步复核发现原文语义并不支持“AMI/HF 常规需要血培养”。

### 根因

早期骨架抽取把同章节或邻近段落里的实体误挂到了当前疾病。只检查“有没有 evidence_text”会漏掉这种错误，因为证据文本本身存在，但证据语义不属于当前疾病场景。

### 防复发规则

1. 关系级证据必须同时满足：
   - 原文非空；
   - 原文主体与当前疾病一致；
   - 原文语义支持该关系类型。
2. 不允许把感染性心内膜炎、风湿性心脏病等条件性场景证据，泛化为 AMI/HF 常规检查。
3. 前端展示检验/检查推荐时，应优先读取疾病关系级证据；关系被删除或标记 blocked 后不得展示。
4. 后续质量体检需要增加“证据语义错配”抽检，而不是只做字段完整性校验。

### 本轮处理

`BATCH-CARD-LAB-CLEANUP-20260713-001` 删除 2 条误归属 `requires_lab_test` 关系，保留 LabTest 与原证据节点。

## 2026-07-13 17:30:00｜空证据检验项目不能用“同名泛化证据”硬补

### 典型表现

`肾功能`、`血糖`、`BNP/NT-proBNP`、`动脉血气分析` 等检验项目在旧骨架中已有 LabTest 节点，但部分 Evidence 为空壳，导致前端看似有检验项目，实际缺少可追溯原文依据。

### 根因

早期骨架建设更重“有没有这个检验项目”，没有稳定区分：

```text
LabTest 节点自身的原文依据
Disease -> requires_lab_test 这条疾病关系的疾病场景依据
LabTest -> ExamIndicator 下级指标依据
```

如果只看到同名检验项目就补证，会把“其他疾病/其他场景”的证据错误包装成当前疾病证据。

### 防复发规则

1. 补 LabTest 原文证据时，必须保存非空 `evidence_text/original_text`。
2. 补疾病场景关系证据时，证据文本必须能支撑该疾病为什么需要该检验。
3. 同名检验项目可以共享指标层关系，但不能共享疾病场景结论。
4. `血培养` 这类跨感染/心内科场景项目，如果只在急性心包炎等资料中出现，不得硬补到 AMI 或心衰。
5. 以后生成批次时，审计必须同时检查：
   - `LabTest` 是否有非空原文证据；
   - `LabTest -> ExamIndicator` 是否存在；
   - 疾病关系是否有可解释证据；
   - 无直接证据的项目是否进入阻断清单。

### 本轮处理

`BATCH-CARD-LAB-EVIDENCE-20260713-001` 已补 20 个节点、49 条关系，服务器 postcheck 通过；剩余 2 条血培养被阻断，不硬补。

## 2026-07-13 14:20:00｜检验指标补链必须区分“有证据可补”和“空证据缺口”

### 典型表现

AMI 修复后，继续扫描心肌病、心力衰竭、心律失常时发现：很多 `LabTest` 没有 `lab_test_has_indicator`，但并不是每一项都能直接补。有些项目有大量非空证据，例如甲状腺功能检查、血钾、血镁、INR、凝血功能检查；有些项目虽然挂了 Evidence 节点，但 `evidence_text/original_text/evidence_summary` 为空，例如部分 `肾功能`、`血糖`、`脑钠肽/BNP/NT-proBNP`。

### 根因

早期骨架回填把“需要检查某检验项目”先建成了 `LabTest`，但没有稳定抽取下级指标；部分 Evidence 只保留了来源壳，缺少可读原文。这会造成两种风险：一是前端无法下钻；二是为了覆盖率硬补后，会把没有原文依据的指标包装成 CDSS 依据。

### 防复发规则

1. 不新增 `LabTestIndicator`，检验指标仍使用 `ExamIndicator`。
2. `LabTest -> lab_test_has_indicator -> ExamIndicator` 可以是共享关系，不要把关系属性强行绑定到某一个疾病。
3. 只有存在非空 `evidence_text/original_text/evidence_summary` 的证据，才允许自动补链。
4. 对 `肾功能`、`血糖`、`BNP/NT-proBNP`、`动脉血气分析` 这类旧骨架空证据，不得硬补；必须回教材或指南原文补证据后再补。
5. 质量门禁必须统计：有 `requires_lab_test` 的疾病中，`lab_test_has_indicator` 覆盖率、整病种缺失数、部分缺失数。

### 已执行修复

已通过 `BATCH-CARD-LABIND-SAFE-20260713-001` 新增 8 个标准检验指标、32 条关系并导入 Neo4j。服务器硬闸门：非 KGNode=0、指标无证据=0、关系无 evidence_ids=0、重复语义边=0。

## 2026-07-13 00:35:00｜检验指标不要新造 LabTestIndicator，必须通过关系路径区分

### 典型表现

前端或审计脚本看到 `LabTestIndicator=0`、`LabTest.indicators=[]` 时，容易误判为 Schema 缺少“检验指标实体类型”。实际 Schema 当前只保留 `ExamIndicator`，同时承载检查指标和检验指标。分类不是靠实体类型区分，而是靠上游路径区分。

### 根因

早期数据生成时只补了部分 `Exam -> exam_has_indicator -> ExamIndicator`，但没有同步补 `LabTest -> lab_test_has_indicator -> ExamIndicator`。这会导致 AMI 等疾病虽然有肌钙蛋白、CK-MB、血常规等检验项目，却无法下钻到“肌钙蛋白升高、CK-MB升高、白细胞计数升高”等可解释指标。

### 防复发规则

1. 不新增 `LabTestIndicator`，除非未来 Schema 正式版本变更。
2. 前端查询检验指标必须走：

```text
Disease -> requires_lab_test -> LabTest -> lab_test_has_indicator -> ExamIndicator
```

3. 前端查询检查指标必须走：

```text
Disease -> requires_exam -> Exam -> exam_has_indicator -> ExamIndicator
```

4. `LabTest.indicators` 属性不是权威来源，不能作为是否有检验指标的判定依据。
5. 每个有 `requires_lab_test` 的疾病，审计时必须统计 `lab_test_has_indicator` 覆盖率。
6. 没有原文证据的检验项目不得硬补指标；应进入缺口清单，避免伪造 CDSS 依据。

### 已执行修复

AMI 已通过 `BATCH-CARD-CAD-AMI-LABIND-20260713-001` 补入 11 个检验指标、15 条检验项目到指标关系，并挂教材证据。全局扫描显示 75 个有检验项目的疾病中，34 个完全缺失检验指标、35 个部分缺失，后续需要分批治理。

## 2026-07-12 21:46:00｜教材章节名不等于图谱疾病名，必须先映射再抽取

### 典型表现

冠心病剩余病种批量精修时，如果按图谱疾病名直接匹配教材骨架，会漏掉关键内容：教材章节名是“不稳定型心绞痛和非 ST 段抬高型心肌梗死”，但图谱疾病节点是 `不稳定型心绞痛` 和 `非ST段抬高型心肌梗死` 两个；教材写“隐匿型冠心病”，图谱标准名是 `隐匿性冠心病`。

### 根因

教材目录和图谱疾病节点不是一一同名关系。教材经常按章节合并讲述，图谱则按临床应用对象拆成疾病、亚型或综合征。如果直接按名称等值匹配，会造成“看起来教材没有，实际是映射没做”的假缺口。

### 防复发规则

1. 新批次进入 D6 教材骨架抽取前，必须先建立 `教材章节名 -> 图谱疾病标准名/别名/疾病编码` 映射。
2. 合并章节必须允许一章映射多个疾病，例如 UA/NSTEMI。
3. 同义章节名必须进入 alias 字典，例如“隐匿型冠心病 -> 隐匿性冠心病”。
4. 未映射命中的缺口不得直接判断为“教材未覆盖”，必须先输出映射缺口矩阵。

### 已执行修复

冠心病剩余病种教材骨架精修批次已按上述规则生成并导入 Neo4j：节点 461、关系 859；服务器硬闸门全 0。

## 2026-07-12 21:45:00｜泛化候选不能为了凑数进入图谱

### 典型表现

D6 教材候选中存在“因此需要鉴别诊断”“与其他疾病的鉴别诊断参见稳定型心绞痛部分”“结合年龄和存在冠心病危险因素”等泛化或跳转式文本。如果直接生成实体，会在前端形成看似有节点、实际没有可用临床内容的空壳。

### 根因

自动抽取器会把章节过渡语、表格标题、跳转说明误识别为临床实体。早期如果只看数量，会误以为覆盖增加；但对 CDSS 来说，这类节点不可推理、不可展示、不可审核。

### 防复发规则

1. 鉴别诊断实体必须是疾病、病理状态或明确鉴别规则，不得是“需要鉴别诊断”这类泛化语。
2. 短实体类型（症状、体征、检查、检验、药物、手术、危险因素、并发症）名称不得过长；长句必须进入 Evidence 或 description，不得作为节点名。
3. 本地硬闸门必须检查 `has_related_entity=0`、短实体过长=0、诊断标准无明细=0、Evidence 无原文=0。
4. 被过滤的候选必须计入审计矩阵，不能静默丢弃。

### 已执行修复

冠心病剩余病种批量精修中，泛化候选已过滤并写入覆盖矩阵；服务器 postcheck 显示：`generic_has_related_entity_count=0`、`short_entity_too_long_count=0`、`diagnosis_criteria_without_component_count=0`。

## 2026-07-12 21:50:00｜ClinicalRule 必须保留原文证据，组件类不得用泛化关系直挂疾病

### 典型表现

STEMI 精修前，部分规则只有规则名称、触发条件、适用人群、处理后逻辑等结构化字段，但缺少“教材/指南原文段落”。医生和开发同事看到规则时无法判断这条规则从哪段原文抽取而来。另一个问题是组件类实体容易被泛化关系直接挂到疾病下，导致前端出现“诊断标准底下还是诊断标准”“检查指标孤立显示”“治疗方案只有标题”等空壳式下钻。

### 根因

早期为了快速形成图谱节点，把“机器可执行字段”和“原始医学依据”混在同一个节点里；当抽取器无法稳定处理原文时，就只留下处理后字段。组件类实体又缺少明确承接父节点，导致用 `has_related_entity` 兜底，后续前端很难解释。

### 防复发规则

1. 所有 `ClinicalRule` 必须存在 `derived_from -> Evidence`，且 Evidence 必须保存可读原文段落或表格原始内容摘要。
2. `rule_logic`、`trigger_condition`、`required_patient_facts` 是处理后字段，不得替代原文证据。
3. `DiagnosisCriteriaComponent` 只能由 `DiagnosisCriteria -> has_diagnostic_component` 承接，不得用泛化关系直接挂疾病。
4. `ExamIndicator` 只能由 `Exam -> exam_has_indicator` 承接，不得孤立挂疾病。
5. 禁忌证、慎用、排除条件、阻断逻辑必须显式进入 `Contraindication + ClinicalRule + blocks_action`，不得写成泛化模板语。
6. 本地硬闸门必须检查：ClinicalRule 原文证据覆盖、泛化关系数量、模板化排除语、诊断标准明细、鉴别诊断明细、具体药物和禁忌阻断。

### 已执行修复

STEMI 教材骨架精修包已按上述规则生成并通过本地硬闸门：`ClinicalRule` 原文证据覆盖通过，`has_related_entity=0`，模板化排除语=0，诊断标准明细=5，鉴别诊断=7，具体溶栓药物=5，禁忌证=10，阻断关系=4。

## 2026-07-12 16:45:00｜历史旧脚本和旧文件不能无限保留

### 典型表现

2026 年 6 月第一批冠心病批次中仍保留旧聚合 JSON、旧 CSV、页面渲染样本和一次性最终化脚本；根目录还保留早期 superpowers 计划草稿 `docs/`。这些内容不参与当前 V2.1 SKILL、Schema、证据链或入库流程，但会干扰目录判断和后续执行效率。

### 根因

早期批次为了快速试错保留了多种中间格式和临时脚本，后续虽然形成了 JSONL、G0/G1/G2/G3、kg_pipeline、批次台账等正式流程，但没有同步建立“历史资产淘汰规则”。

### 防复发规则

1. 旧批次不能整目录删除，必须先区分“正式证据/可再生中间物/一次性脚本/历史草稿”。
2. `nodes_final.jsonl`、`relations_final.jsonl`、来源清单、证据索引、质量审计默认保留。
3. 旧 CSV、旧聚合 JSON、可重建渲染样本、无依赖的一次性脚本可以删除。
4. 删除前必须验证目标路径位于项目工作目录内。
5. 删除后必须更新脚本资产台账、步骤记录、交接记录；不能只删文件不留治理结论。

### 已执行修复

已删除 `docs/`、`scripts/finalize_coronary_batch.py`、冠心病 6 月第一批旧 CSV/聚合 JSON/render_samples，累计释放约 431.12 MB；保留正式 JSONL、来源清单、证据索引和质量审计。

## 2026-07-12 14:20:00｜计划、台账、SKILL 的疾病顺序必须自动校验

### 典型表现

主 SKILL、月度计划、批次台账都包含本月疾病目标，但某个文件的顶部列表顺序与正式建设顺序不一致。例如月度计划正文曾出现“心肌病、冠心病……”，而批次台账和执行顺序要求是“冠心病、心肌病……”。

### 根因

之前治理脚本只检查“目标是否存在”，没有检查“目标顺序是否一致”。长期运行时，多个文件手工同步容易出现顺序漂移，导致下一阶段执行入口误判。

### 防复发规则

1. 月度目标必须在主 SKILL、月度计划、批次台账中保持同一顺序。
2. 本月心血管内科顺序固定为：冠心病、心肌病、急性心肌梗死、心力衰竭、高血压、心律失常、起搏治疗相关疾病、瓣膜病、肺动脉高压。
3. `校验SKILL治理_validate_skill_governance.py` 必须检查顺序，不允许只检查目标存在。
4. 如果用户调整顺序，必须同步更新主 SKILL、月度计划、批次台账和治理脚本中的期望顺序。

### 已同步位置

- `治理脚本_governance_scripts/校验SKILL治理_validate_skill_governance.py`
- `项目管理中心_project_management/14_心血管内科月度建设计划_202607.md`
- `项目管理中心_project_management/15_SKILL_V2.1精确校验报告_20260712.md`

## 2026-07-12 09:29:41｜历史全书回捞候选池不能批量复用

### 典型表现

历史候选池中存在大量“看似可归并”的症状、体征、检查、治疗等实体，但证据片段常存在章节坐标不对齐、疾病名未出现、否定/禁忌上下文、泛化实体等问题。

### 根因

历史回捞候选是早期宽松抽取结果，不是当前 D6 骨架基线下的严格证据事实。它可以提升召回线索，但不能替代“当前骨架 + 原文证据 + G1/G2审计”的正式抽取流程。

### 防复发规则

1. 历史候选池最高用途是“重抽取线索库”，不得直接生成入库 delta。
2. 任意历史候选必须回到教材/PDF原文，重新抽取证据、实体、关系和槽位。
3. 只有当前流程重新生成并通过 G1/G2 审计的数据，才允许进入服务器导入。
4. 抽样核查报告中如 `direct_batch_reuse_allowed=false`，后续流程必须阻断批量复用。

### 已同步位置

- `骨架质量闭环_skeleton_quality_loop/20260712_历史候选池复用抽样核查/历史候选池复用抽样核查报告_20260712.md`
- `治理脚本_governance_scripts/抽样核查历史候选池复用价值_sample_candidate_reuse_audit.py`
- `治理脚本_governance_scripts/校验历史候选池复用抽样_validate_candidate_reuse_sample.py`

## 2026-07-12 08:39:56｜不能把历史候选池问题当成当前骨架问题

### 典型表现

分析全书回捞候选时，发现疾病名映射、章节坐标和证据文本噪声问题，如果不区分产物时间，容易误判为“本周新骨架仍有严重问题”，造成无效返工。

### 根因

当前 D6 教材骨架是 2026-07-09 之后的新基线，而 `textbook_fullbook_backfill_index.csv` 是 2026-06-25 的历史候选池。两者不是同一阶段产物，不能用旧候选池质量反向否定当前骨架。

### 防复发规则

1. 每次质量审计必须先列出输入文件 mtime 和产物阶段。
2. 只有当前基线之后生成的正式骨架/入库产物，才计入当前质量问题。
3. 当前基线之前的旧候选池，只能标记为“历史候选池归并风险”，不得作为当前骨架扣分。
4. 如需复用历史候选，必须经过名称映射、证据文本质量、章节坐标口径三道审计，再进入抽样复核。

### 已同步位置

- `治理脚本_governance_scripts/对齐心血管骨架疾病名称与证据坐标_align_cardiology_skeleton_names.py`
- `治理脚本_governance_scripts/校验疾病名称与坐标对齐_validate_name_coordinate_alignment.py`
- `骨架质量闭环_skeleton_quality_loop/20260712_心血管内科疾病名称与证据坐标对齐/疾病名称与章节坐标对齐报告_20260712.md`

## 2026-07-12 08:04:56｜全书回捞候选不能直接归并到D6教材槽位

### 典型表现

全书回捞索引中已有大量症状、体征、检查、治疗等深层实体，但疾病名与 D6 教材章节名不完全一致，且 `line_number` 与 `docx_start_para/docx_end_para` 疑似不是同一坐标系。

### 根因

全书回捞、D6教材矩阵、图谱Disease节点、alias字典之间缺少统一疾病名称映射和统一章节坐标口径。

### 防复发规则

1. 未完成疾病名称标准化映射前，不得把全书回捞候选直接归并到疾病槽位。
2. 未确认 `line_number` 与 D6 段落坐标同源前，不得按段落范围自动判定候选归属。
3. 先输出名称对齐审计和疑似跨章节噪声清单，再进入 G1/G2 候选审计。

### 已同步位置

- `项目管理中心_project_management/05_全局错误指纹索引_error_fingerprint.csv`
- `骨架质量闭环_skeleton_quality_loop/20260712_心血管内科教材骨架审计/心血管内科教材骨架质量闭环总报告_20260712.md`
- `治理脚本_governance_scripts/归并心血管骨架深层实体_reconcile_cardiology_skeleton_candidates.py`

## 2026-07-11 23:55:00｜长文档膨胀导致上下文压缩后难接续

### 典型表现

1. 主 SKILL、步骤记录、踩坑日志越来越大，读取慢、token 消耗高。
2. 同一规则同时散落在 SKILL、步骤记录、踩坑日志、方案文件中，后续难判断哪个是最新。
3. 换账号或上下文压缩后，容易重新问、重新解释、重复犯错。

### 根因

把“执行总纲、历史过程、事故复盘、软件化索引”混在同一长文档中，没有分层。

### 防复发规则

1. 主 SKILL 只保留总纲和硬闸门。
2. 细节进入 `技能文档_skill_docs/` 附录。
3. 历史步骤和踩坑按月归档。
4. 可复发错误必须进入结构化错误指纹索引。
5. 每轮结束必须更新 `当前进度与后续计划_交接.md`。

### 已同步位置

- `AI自动化工具-文献指南解析.md` V2.0
- `技能文档_skill_docs/附录G_日志归档错误指纹与交接_handoff_logs.md`
- `项目管理中心_project_management/05_全局错误指纹索引_error_fingerprint.csv`
## 2026-07-14 07:25:00｜RecommendationStatement 缺核心链路优先回连已有 code，不重新造证据

### 典型表现

全库体检显示 `RecommendationStatement` 缺核心链路 344 条。复核后发现其中 291 条并不是缺证据内容，而是节点属性里已有 `primary_evidence_code`，目标 Evidence 节点也存在且非空，但缺少 `RecommendationStatement -> supported_by_evidence -> Evidence` 关系。

### 根因

CDSS 决策层升级时，推荐节点属性和动作/指南关系生成较完整，但证据关系没有同步补齐。旧审计只看关系层，因此把这类“属性已含证据 code、关系未回连”的节点判成缺证据。

### 防复发规则

1. 生成 `RecommendationStatement` 时，`primary_evidence_code`、`primary_guideline_code`、`action_code` 不能只写属性，必须同步落为图关系。
2. 可自动修复的边界：属性 code 非空、目标节点存在、Evidence 文本非空。
3. 不可自动修复的边界：缺 `primary_evidence_code`、目标节点不存在、Evidence 空文本；这类必须进入 blocker，不能根据推荐名称猜证据。
4. 后续审计必须同时输出“属性可回连缺口”和“真正内容缺口”，否则会把可机械修复问题误判成临床内容缺失。

### 本轮处理

`BATCH-CARD-RS-CORELINK-20260714-001` 已回连 293 条关系，RecommendationStatement 缺核心链路 344 → 53。

## 2026-07-14 07:25:00｜诊断标准无明细不能只按标题补，必须有非空 Evidence

### 典型表现

24 条 `DiagnosisCriteria` 无 `has_diagnostic_component`。其中 18 条已有非空 Evidence，可以生成“原文诊断依据组件”；6 条传导阻滞类诊断标准只有标题或共用节点，无非空证据。

### 防复发规则

1. 有 Evidence 的诊断标准可先生成 `DiagnosisCriteriaComponent` 承载原文依据，解决前端下钻和空壳问题。
2. 只有标题、没有非空 Evidence 的诊断标准不得自动生成组件。
3. “原文诊断依据组件”只是第一层补齐，不等同于最终可执行阈值规则；后续仍需按专病精修成 ClinicalRule / ExamIndicator / ThresholdRule。

### 本轮处理

`BATCH-CARD-DXC-COMP-20260714-001` 已新增组件 18 个、关系 36 条，DiagnosisCriteria 无明细 24 → 6。
## 2026-07-14 07:39:04｜D-二聚体共享检验节点缺指标/证据与VTE串段污染

### 问题

- 全库体检发现 `required_lab_without_indicator_or_evidence=2`，均指向共享 LabTest：D-二聚体。
- 进一步排查发现：
  - LabTest 节点本身缺少 `lab_test_has_indicator`。
  - LabTest 关联 Evidence 多为空文本，导致“疾病需要检验”关系虽然存在，但医生无法看到指标含义和证据。
  - `DIS-CARD-VTE` 存在历史串段污染，不能把所有 VTE 关系当作可信骨架直接展示。

### 本次修复

- 新增 D-二聚体 3 个指标节点并挂接原文证据：
  - 低度临床可能性疑诊急性PTE：D-二聚体＜500μg/L可基本排除。
  - ＞50岁年龄校正临界值：年龄×10μg/L。
  - 肺栓塞鉴别场景：D-二聚体升高但需结合临床和影像。
- 急性心包炎关系补充说明：D-二聚体用于肺栓塞鉴别，不是急性心包炎确诊指标。

### 复用规则

- 共享检验项目不能只有 LabTest 名称，至少需要：
  - `LabTest -> lab_test_has_indicator -> ExamIndicator`
  - `LabTest/ExamIndicator -> supported_by_evidence -> Evidence`
  - 若用于鉴别诊断，疾病-检验关系必须写清 `clinical_context` 和 `usage_note`。
- 发现某疾病节点存在大规模串段污染时，不能靠局部补丁掩盖；必须另立“疾病骨架重建/污染清理”批次。
## 2026-07-14 13:17:01—错误指纹：疾病级总治疗方案、具体治疗方案、风险分层不能混用

### 典型表现
冠心病总精修时发现 11 条“治疗方案无下游动作”缺口。复核后确认其中 9 条疾病级总治疗方案已经连接 `ClinicalPathway`，属于审计口径误报；真正问题是：

1. `直接PCI治疗` 是具体治疗方案，但没有 `includes_procedure -> 经皮冠状动脉介入治疗`。
2. `泵衰竭与血流动力学评估` 实际是风险分层/血流动力学评估，却被挂成 `TreatmentPlan` 和 `has_treatment_plan`。

### 根因
早期抽取时把“治疗章节中的内容”全部归入治疗方案，未区分：

- 疾病级总治疗方案：应该连接临床路径或下级具体方案。
- 具体治疗方案：必须连接药物、操作、禁忌、时机、随访等可执行动作。
- 风险分层/评估内容：应归入 `RiskStratification` 或 `ClinicalRule`，不能为了通过治疗方案审计强行挂药物。

### 防复发规则
1. `TreatmentPlan` 不得靠名称通过审计，必须有可执行下游或临床路径。
2. 名称含“评估、分层、血流动力学、Killip、Forrester、风险”的节点，默认先判定为 `RiskStratification/ClinicalRule`，不得直接进入治疗方案。
3. 疾病级总治疗方案不要复制疾病下全部药物；否则会污染前端展示和 CDSS 推荐。
4. 具体方案如“直接PCI治疗、溶栓治疗、抗凝治疗、抗血小板治疗”必须连接具体操作或具体药物。
5. 修复完成后必须回归：治疗方案可执行性缺口=0、规则缺证据=0、规则缺文本=0，再允许本轮节点转正式。

### 本轮固化
- `直接PCI治疗` 已补 `includes_procedure -> 经皮冠状动脉介入治疗`。
- `泵衰竭与血流动力学评估` 已改为 `RiskStratification`。
- 36 条规则已补同病种证据链。
- 38 个本轮测试通过节点已转 `formal_cdss_ready=true`。
## 2026-07-15——DOCX随访资料不能只解析不入证据索引

### 触发场景

起搏治疗相关疾病批次中，3 份起搏器护理/出院指导 DOCX 已完成 clean text 解析，但没有进入 `guideline_evidence_index.jsonl`，导致随访 required 缺口。

### 根因

证据抽取器当前主要面向 PDF/指南段落，未把 DOCX 患教/护理材料中的“定期随访、术后管理、出院指导”结构化为 Evidence。

### 固化规则

1. 后续批次 G1/G2 前必须检查：纳入 DOCX 是否只有 clean text、没有 Evidence。
2. 护理指导、出院指导、患者教育材料可用于随访、护理、注意事项、患者教育等槽位，但不得替代指南推荐等级。
3. DOCX 证据写入时 `source_type` 应明确标记为 `hospital_patient_education` 或同类来源，推荐等级和证据等级保持未分级。

### 本次处理

已在 `scripts/修复起搏治疗批次质量_20260715.py` 中，将永久起搏器出院指导的“定期随访”段落生成随访证据，补齐 6 个疾病的起搏器植入后随访。

## 2026-07-15——缩写不能作为临床主名称

### 触发场景

起搏治疗批次中出现 AVB、BBB、BRADY、SINUS、SND 作为目录节点主名称。

### 固化规则

主名称必须是医生可读中文标准名；英文缩写只能放在 `abbr` 或 `aliases`。例如 AVB 应显示为“房室传导阻滞”，AVB 放入别名/缩写。

### 本次处理

已将相关缩写目录节点改为中文主名，并保留英文缩写作为别名。

## 2026-07-15——治疗方案必须挂具体药物/操作/随访动作

### 触发场景

起搏治疗批次中，一度/二度/三度房室传导阻滞、窦房传导阻滞的治疗方案节点最初只有方案标题，缺少下游具体动作。

### 固化规则

`TreatmentPlan` 不能停在“治疗方案”标题，必须继续连接 `includes_medication`、`includes_procedure`、`has_follow_up`、`has_timing` 等可执行实体。

### 本次处理

已补齐临时心脏起搏、永久起搏器植入、阿托品、异丙肾上腺素、病因治疗、定期随诊观察，并补齐适用人群、排除/禁忌、剂量/相互作用和证据链字段。
## 2026-07-15 22:45:00——服务器入库必须检查诊断标准下钻组件

问题：

- 4 个新完整批次本地质量闸门通过，但服务器 postcheck 发现 17 个 `DiagnosisCriteria` 缺少 `has_diagnostic_component` 下钻明细。
- 这说明本地闸门覆盖过“是否有诊断标准”，但没有强制覆盖“诊断标准是否可下钻到症状、体征、检查、检验或规则组件”。

固化规则：

1. 所有 `DiagnosisCriteria` 必须至少有 1 条 `has_diagnostic_component`。
2. 优先使用同疾病已有的症状、体征、检查、检验实体作为诊断标准下钻组件，不允许创建空壳“诊断明细”节点。
3. 入库后服务器 postcheck 必须包含：`DiagnosisCriteria` 无组件数 = 0。

本次处理：

- 为 4 个完整批次补齐 309 条诊断标准下钻组件关系。
- 二次服务器专项复测显示：17 个诊断标准无明细缺口全部归零。

## 2026-07-15 22:45:00——Evidence 批量写入要小批量，医生端只展示主证据

问题：

- Evidence 节点数量多、文本较长，批量写入 Neo4j 时大批量请求可能触发 HTTP 500。
- Evidence 是证据追溯层，不应在医生端按疾病一次性全部展示。

固化规则：

1. Evidence 节点保留是合理的，但必须做去重、孤立证据检查和长文本比例统计。
2. Neo4j 写入 Evidence 节点和证据关系时使用小批量；失败时自动拆分定位。
3. CDSS 展示证据必须从推荐动作或推荐陈述出发，只展示支持该推荐的 1–3 条主证据。
4. 后续可评估“图谱保存摘要、页码、段落、哈希；原文长文本留外部文件/文档库”的证据瘦身方案。

## 2026-07-15 20:55:00——外部权威 PDF 必须集中到 01_原始下载_raw_downloads

问题：

- 外部补源 PDF 不能散落在“外部权威”根目录，否则后续批次登记、来源追溯、文件同步和备份都会混乱。

固化规则：

1. 所有外部权威原始下载 PDF 统一保存到：`E:\BigMouse\0.CDSS文献诊疗指南材料PDF\心血管内科\外部权威\01_原始下载_raw_downloads`。
2. 文件命名继续采用：`专科+疾病大类+疾病+权威来源+日期.pdf`。
3. 批次目录只登记引用来源，不复制散落文件；后续入库必须能从 manifest 追溯到原始下载目录。

## 2026-07-15 20:55:00——上位病种证据必须补疾病锚点，不能只靠原文出现完整病名

问题：

- 急性主动脉综合征属于上位病种，指南原文可能写“主动脉夹层”“B 型主动脉夹层”“胸主动脉扩张”等下位或相关概念，不一定出现完整中文病名“急性主动脉综合征”。
- 如果只靠证据原文包含疾病名称，会误判为疾病相关性失败。

固化规则：

1. 上位病种引用下位疾病或子场景证据时，Evidence 或关系来源记录必须写入 `disease_code`、`disease_name` 上下文锚点。
2. 不允许为了通过审计而篡改原文证据；只能补上下文归属元数据。
3. 疾病锚点用于说明“这条证据归属于哪个疾病闭环”，原文仍保持原始摘录。

## 2026-07-15 20:55:00——CDSS 随访推荐字段必须使用审计认可字段名

问题：

- 本轮主动脉与外周动脉批次曾使用 `exclusion_or_contraindication` 表达排除/禁忌，但审计标准只识别 `exclusion_criteria`、`contraindication`、`contraindications` 等字段。
- `clinical_ready` 是可读表达，但审计标准要求正式临床签收状态，如 `clinical_batch_signed_off`。

固化规则：

1. 关系作为 CDSS 推荐进入正式层时，适用人群字段使用 `applicable_population`。
2. 排除/禁忌字段优先使用 `exclusion_criteria`，药物/操作禁忌可补 `contraindication`。
3. 临床批量签收状态使用 `clinical_batch_signed_off`，不要再用含义模糊的 `clinical_ready`。
## 2026-07-16 11:40:00——别名存在不等于旧节点已归并，主数据必须做服务器级复核

问题：
- 术语字典里已经有 `PCI -> 经皮冠状动脉介入治疗`，但服务器仍残留旧 `PCI` 节点和疾病专属重复节点，前端在“同维度其他实体”里仍可能看到旧节点。
- 同类问题不仅存在于 PCI，还存在于 `CT/CTA/CMR/Holter/BNP/NT-proBNP/IVUS/OCT/INR/PBPV/TAVI` 等缩写节点。
- 仅把标准节点改成中文全称不够，如果旧节点没有迁关系并停用，仍会污染前端展示和 CDSS 查询。

固化规则：
1. 中文全称必须作为 `name/display_name/preferred_name`；英文缩写只能进入 `abbr` 或 `aliases`。
2. 别名可以保留，但必须保留在正确的标准节点上；不得把不同检验混到同一个别名集合里，例如 BNP 与 NT-proBNP 必须拆开。
3. 修复缩写或同义词问题时，必须做服务器级全库复核，不能只看本地术语字典。
4. 旧节点不物理删除；先迁移所有入边/出边，再标记 `status=deprecated`、`duplicate_replaced_by=标准节点code`。
5. 前端查询必须默认过滤 `status=deprecated` 的节点。
6. 同类型同名重复也是主数据污染，Procedure/Exam/LabTest 必须在入库后复测中强制检查。

本次处理：
- 服务器有效缩写主名节点归零。
- Procedure/Exam/LabTest 同类型同名重复归零。
- AMI 旧 PCI 节点剩余关系归零。
- BNP 与 NT-proBNP 已拆分为两个标准检验项目，缩写保留为别名。

复用到后续批次：
- 新病种入库后必须执行“主数据质量闸门”：缩写主名、同名重复、旧节点残留关系、停用节点过滤。
- 发现前端显示异常时，先查服务器有效节点和关系，不先假设是前端问题。

## 2026-07-16 11:40:00——AMI、STEMI、NSTEMI 层级不能只靠平铺疾病列表表达

问题：
- 图谱原来只有“AMI、STEMI、NSTEMI 都属于 ACS 谱系”，缺少“AMI 下钻到 STEMI/NSTEMI”的明确表达。
- 前端按 Disease 平铺时，会让用户误以为 AMI 与 STEMI/NSTEMI 是同一层，没有父子/分型关系。

固化规则：
1. DiseaseCategory/DiseaseSubcategory 负责大类与谱系目录。
2. Disease 负责可独立诊疗的疾病对象。
3. DiseaseClassification 负责疾病内部临床分型；例如“急性心肌梗死 -> STEMI/NSTEMI”。
4. 如果某个分型也需要独立诊疗，可以同时保留 Disease 节点，但必须在分型节点上写 `maps_to_disease_code`，方便前端跳转到对应专病。

本次处理：
- 补齐 `急性心肌梗死 -> has_classification -> STEMI/NSTEMI`。
- STEMI/NSTEMI Disease 节点增加 `parent_code=DIS-CARD-CAD-AMI`，供前端树形展示使用。
- 不新增乱关系，不改变 STEMI/NSTEMI 的 Disease 身份。
## 2026-07-17 14:00:00——英文指南补充批次必须先补英文全称和缩写别名

问题：

- 未解析 PDF 补充批次中多份指南为英文或中英混排。
- 如果 controlled_vocabulary 只有中文疾病名，证据抽取器会因为找不到疾病锚点而抽取不到证据。
- 首轮 7 个补充批次只有 31 条证据，5 个批次为 0，就是这个问题。

固化规则：

1. 英文指南、国际指南、中英混排指南，G0 后必须检查 controlled_vocabulary 是否包含英文全称和常用缩写。
2. 缩写不得作为标准名称；标准名称仍用中文，英文全称和缩写放入 name_en、abbr、aliases。
3. 证据抽取后必须检查 evidence_count；英文指南证据为 0 时，不得进入图谱实例生成和入库。
4. 补充指南批次不是完整专病闭环批次，不能因为 required 路径缺口就反复硬补；应作为增量证据合并到已有标准疾病节点。

本次处理：

- 为 ACS/AMI/STEMI/NSTEMI/UA、VHD/AS/AR/MS/MR、AF/AFL/VA/SCD、HCM/DCM/RCM/ACM、LVAD/HF/HTN 等补齐英文别名。
- 证据数从 31 提升到 3021，再通过疾病编码修正后达到 5851。
- 本次不写 Neo4j。

## 2026-07-17 13:55:00——补充批次必须补疾病大类编码和专病编码，不能只写中文 category_code

问题：

- 补充启动脚本最初把 `category_code` 写成“冠心病、心肌病、高血压”等中文名。
- 构建图谱后出现 `code=冠心病` 的 DiseaseCategory 节点，触发技术显示名错误。
- 同时 `disease_code` 为空，会导致只生成疾病大类，不生成具体疾病节点。

固化规则：

1. `category_code` 必须是稳定技术编码，中文名只能放入 name/display_name/preferred_name。
2. `disease_code` 必须优先复用服务器/本地已有标准疾病编码。
3. 补充批次如果无法确认疾病编码，只能做候选证据，不得直接入库。
4. G0 后必须抽检 `scope_taxonomy.csv`，确认 category_code 和 disease_code 不为空、不使用纯中文疾病名当编码。

本次处理：

- 已补 `CATEGORY_CODE_MAP` 和 `DISEASE_CODE_MAP`。
- 重建后技术显示名错误归零。
