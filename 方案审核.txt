**发给Codex的内容：**

> Claude审核了Skill方案V2.0和已交付的两份CSV（`01_心血管内科三层体系标准化_nodes.csv`、`08_疾病诊疗路径闭环完整度_audit.csv`），发现4个需要处理的问题，按优先级排列。在处理完前两项之前，不要继续往下游（实体关系生成）推进。

**问题1（P0，阻断）：心源性休克在三层体系里被复制成了两个不同的Disease节点**

源文件`心血管内科三层知识体系_完整V1.md`里"心源性休克"同时出现在心力衰竭类目和心血管急危重症类目下，导致生成了`DIS-CARD-HF-CS`和`DIS-CARD-CE-CSHOCK`两个disease_id，`name_cn`都是"心源性休克"。这两个节点在`08_`闭环审计文件里有不同的覆盖率结果（一个100%、一个91.67%），意味着它们会各自累积独立的证据候选，造成真实的内容冗余甚至矛盾。

请回答并处理：

- 这是源文件本身的分类设计（心源性休克作为综合征，确实需要同时被两个大类引用），还是应该合并成一个实体？
- 如果应该保留为多重分类视角，要求：保留一个主节点（建议保留在更贴切的大类下，由医学团队判断HF还是CE），删除重复节点，改用多条`belongs_to_category`关系把同一个Disease节点挂到两个DiseaseCategory下，而不是复制实体本身。
- 如果合并，需要同步检查`08_`闭环审计文件里这两行的证据候选是否需要合并去重，避免合并后同一份证据被计两次。
- 顺带请求：扫描`心血管内科三层知识体系_完整V1.md`全文，确认是否还有其他疾病名称（`name_cn`）重复出现在不同DiseaseCategory下但生成了不同disease_id的情况，不要只修这一个,要交一份完整清单。

**问题2（P0，阻断）：CAD和VHD两个code在DiseaseCategory节点和Disease节点之间冲突**

`01_`节点CSV里，`code=CAD`同时被用在"冠状动脉疾病"（DiseaseCategory，`CAT-CARD-CAD`）和"冠状动脉粥样硬化性心脏病"（Disease，`DIS-CARD-CAD-CAD`）两条记录上；`code=VHD`同样被"瓣膜性心脏病"的大类和具体疾病两条记录共用。

请回答并处理：

- 如果`code`这一列在Neo4j或前端查询里被当作唯一索引使用，这两组冲突会导致查询歧义，需要先确认`code`是否真的被这样用,如果是，必须立刻改名消歧（比如把具体疾病的code改成`CAD-NOS`或类似命名，区别于大类的`CAD`）。
- 请对全部78行（含两个CSV里出现的全部code）做一次完整的`code`唯一性扫描，不要只改我发现的这两个，交一份`duplicate_code_conflicts.csv`（方案第13节本来就要求这份输出物，但目前没有看到它真的生成过，这次请补上）。

**问题3（P1）：第10节"同名实体归一"质量闸门目前没有真正执行**

方案第10节写了"同名实体归一"是入库前必须验证的一项，但问题1里的两个"心源性休克"节点显然没有被这条规则拦下。说明这条质量闸门目前只是文字描述，没有对应的可执行脚本，或者脚本存在但没检查`name_cn`重复这种情况。

请处理：

- 写一个实际的校验脚本（或在现有流程里补一步），对所有Disease节点按`name_cn`分组，输出任何分组内出现多个不同`id`的情况。
- 把这个脚本的输出固化成一份新的交付物`disease_duplicate_name_report.csv`，作为以后每一轮的标准产物之一，不要这次修完就不再检查。

**问题4（P2，非阻断但建议本轮一并处理）：方案文档第13节的code示例和实际数据不一致**

方案第13节写的示例是"急性冠脉综合征：`DIS-CARD-CAD-ACS`"，但实际`01_`节点CSV里是`DIS-CARD-CE-ACS`（挂在心血管急危重症类下）。这两者归类不一致，说明方案文档示例已经和实际生产数据脱节。

请处理：

- 确认"急性冠脉综合征"这个disease_id到底应该挂哪个DiseaseCategory（这其实是个临床分类问题，建议让医学团队最终拍板,而不是Codex自己决定）。
- 把方案文档第13节的示例同步更新成和实际数据一致的版本，避免后续接入的AI工具（DeepSeek等）照着过时示例写错映射逻辑。

**统一要求：**

四项处理完后，请把改动后的`01_`节点CSV、`08_`闭环审计CSV（如果因为合并节点而变化）、新增的`duplicate_code_conflicts.csv`和`disease_duplicate_name_report.csv`一并发回，我会重新核对，不接受只用文字描述"已修复"。