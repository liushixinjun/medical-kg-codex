# Trae前端提示词：诊疗链路展示压实

## 背景

当前 Neo4j 图谱已经完成“全局诊疗链路语义压实”。前端不要再按旧 CDSS 方式在选择疾病后一次性展示所有检查、检验、治疗、药物和手术，也不要把疾病证据池一次性展示给医生。页面必须按临床诊疗链路展示：疑似疾病、首诊辅助检查、鉴别诊断、鉴别排除检查检验、治疗前评估、正式推荐、推荐证据。

## 改造目标

把 `explore.html` 和疾病详情页改成“场景化展示”。同一个检查或检验项目，要根据关系语义展示在不同位置：

- 服务于疾病首诊：展示在“首诊辅助检查/辅助检验”。
- 服务于鉴别诊断：展示在对应“鉴别对象”的排除依据下面。
- 服务于治疗前置：展示在对应“治疗方案/推荐动作”的治疗前评估下面。
- 服务于正式推荐：只从“推荐陈述 → 推荐动作 → 主证据/支持证据”链路展示。

## 必须使用的新图谱关系

### 1. 疾病首诊辅助检查/检验

查询路径：

```cypher
MATCH (d:Disease {code:$diseaseCode})-[:has_exam_plan]->(plan:ExamPlan)
OPTIONAL MATCH (plan)-[er:includes_exam_item]->(exam:ExamItem)
OPTIONAL MATCH (plan)-[lr:includes_lab_item]->(lab:LabItem)
RETURN plan, er, exam, lr, lab
```

展示要求：

- 使用关系字段 `clinical_stage`、`purpose`、`service_target_name`、`priority_level`。
- `includes_exam_item` 展示为检查项目。
- `includes_lab_item` 展示为检验项目。
- 不要按名称排序截断；优先按 `priority_level`、`clinical_stage`、图谱关系顺序展示。

### 2. 鉴别诊断及排除依据

查询路径：

```cypher
MATCH (d:Disease {code:$diseaseCode})
      -[:has_differential_diagnosis]->(ddx:DifferentialDiagnosis)
OPTIONAL MATCH (ddx)-[:has_differential_rule]->(rule:ClinicalRule)
OPTIONAL MATCH (rule)-[rex:requires_exclusion_exam|requires_exclusion_lab]->(item)
RETURN ddx, rule, rex, item
```

展示要求：

- 一级展示鉴别对象，例如主动脉夹层、肺栓塞、急性心包炎。
- 每个鉴别对象下面展示鉴别规则。
- 每条规则下面展示“需要哪些检查/检验来排除”。
- 检查/检验项必须显示用途：`rex.purpose`，例如“鉴别排除检查”“鉴别排除检验”。
- 如果仍查到“需要考虑以下疾病”“其他病因”这类泛化标题，不要展示，并反馈为图谱异常。

### 3. 治疗方案与治疗动作

查询路径：

```cypher
MATCH (d:Disease {code:$diseaseCode})
      -[:has_treatment_plan]->(tp:TreatmentPlan)
OPTIONAL MATCH (tp)-[tr:includes_medication|includes_procedure|includes_treatment_item]->(action)
RETURN tp, tr, action
```

展示要求：

- `TreatmentPlan` 是治疗策略或方案标题，不等于正式医嘱推荐。
- 方案下面必须展示具体动作：药品、手术/操作、治疗项目。
- 如果治疗方案没有任何下游动作，不进入正式推荐区。
- 药品、手术、治疗项目要使用标准中文名称；英文缩写只显示为别名。

### 4. 正式 CDSS 推荐展示

正式推荐区只能走下面链路：

```cypher
MATCH (rs:RecommendationStatement)-[rr:recommends_action]->(action)
WHERE rs.disease_code = $diseaseCode
OPTIONAL MATCH (rs)-[:supported_by_evidence]->(ev:Evidence)
OPTIONAL MATCH (rs)-[:based_on_guideline]->(gl:Guideline)
RETURN rs, rr, action, ev, gl
```

展示要求：

- 每张推荐卡片展示：推荐动作、适用场景、触发条件、禁忌/排除条件、推荐等级、证据等级、主指南、页码/段落、原文摘要。
- 不要展示疾病全部 Evidence；只展示当前推荐陈述直接关联的证据。
- `has_treatment_plan` 不能作为正式推荐入口。
- `stage_has_available_action` 是路径阶段候选动作，也不能当作正式推荐。
- 当前服务器正式推荐主要通过 `RecommendationStatement.disease_code` 定位疾病；不要只依赖 `Disease -> has_recommendation_statement` 直连关系。

## 页面结构建议

医生选择某个疑似疾病后，页面按下面顺序展示：

1. 疾病概览：定义、标准诊断、具体分型入口。
2. 首诊辅助检查：服务于本疾病初筛或确诊的检查项目。
3. 首诊辅助检验：服务于本疾病初筛或确诊的检验项目。
4. 鉴别诊断：每个鉴别对象单独成卡片。
5. 鉴别排除依据：每个鉴别对象下展示对应检查、检验和结果解释。
6. 治疗方案：只展示方案和下游动作，不作为正式推荐。
7. 正式推荐：只展示 RecommendationStatement 命中的动作和证据。
8. 证据详情：点击推荐卡片后查看主证据和支持证据。

## AMI 样板模拟

选择“急性心肌梗死”后，页面应能展示：

- 首诊辅助检查：心电图、冠状动脉造影、冠脉 CTA、胸部 X 线检查、超声心动图等。
- 首诊辅助检验：心肌损伤标志物、肌钙蛋白、CK-MB、D-二聚体、CRP 等。
- 鉴别诊断：主动脉夹层、肺栓塞、急性心包炎、冠状动脉痉挛等。
- 主动脉夹层鉴别下应显示：主动脉 CTA、超声心动图、胸部 X 线检查、D-二聚体、肌钙蛋白等排除依据。
- 治疗方案下应显示具体动作，例如血运重建 → 经皮冠状动脉介入治疗、冠状动脉旁路移植术；极化液 → 氯化钾、胰岛素、葡萄糖溶液。
- 正式推荐区只展示推荐陈述命中的动作和证据，不展示 1800 多条疾病证据池。

## 心肌病样板模拟

选择“肥厚型心肌病”后，页面应能展示：

- 首诊辅助检查：超声心动图、心电图、心脏磁共振、基因检测等。
- 首诊辅助检验：血常规、心脏生物标志物、利钠肽等。
- 鉴别诊断：冠心病、瓣膜性心脏病、心肌炎等。
- 心肌炎鉴别下应显示：心电图、超声心动图、经皮心内膜心肌活检术、肌钙蛋白、CRP、红细胞沉降率等。
- 治疗方案下展示β受体拮抗剂、抗凝药物等动作，但是否进入正式推荐区必须看推荐陈述链路。

## 状态提示

前端必须区分以下状态：

- 图谱无数据：数据库没有对应链路。
- 有数据但不是正式推荐：只在知识浏览区展示。
- 有正式推荐但缺证据：展示为异常，不进入正式推荐区。
- 查询失败：显示接口或数据库错误。
- 已有结果或已开立：从推荐列表去重，并显示“已有结果/已开立”。

## 验收标准

1. AMI 页面不再把所有检查检验混成一堆，能区分首诊、鉴别、治疗前置、正式推荐。
2. 鉴别诊断能回答“鉴别谁、用什么检查检验、为什么鉴别、依据是什么”。
3. 治疗方案能看到下游动作，但不误当正式推荐。
4. 正式推荐卡片只展示当前推荐自己的主证据和支持证据。
5. 页面不再显示“需要考虑以下疾病”等泛化空壳节点。
6. 如果某疾病没有正式推荐，不得用疾病直连治疗方案冒充正式推荐。
